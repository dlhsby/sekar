/cache
# Auto-regenerated by the Serena MCP plugin on every version bump (comments,
# renamed options) — not meaningful project config. Stop tracking so it stops
# showing as dirty; project.yml still exists on disk and Serena still works.
/project.yml
