# Geography

**Status:** ✅ Active · 🚧 4-level revamp planned · **Backend:** `locations`, `location-types`, `rayons`, `regions`, `kecamatans`, `location-staff-requirements` · **Key ADRs:** ADR-045 (4-level hierarchy + styling), ADR-013 (multi-location), ADR-010 (polygon boundaries)

## Overview
The organizational + spatial hierarchy. Being reworked (UAT) from 3 levels to **4**: City (Surabaya) → **Rayon** (District) → **Region / Kawasan** (NEW) → **Location / Lokasi**. Locations are typed by location-type; location-staff-requirements define minimum staffing. Every level is a "monitoring area" with its own map styling and marker.

## Key decisions
- **Four-level hierarchy** (ADR-045) — new `regions` (Kawasan) entity between rayon and location; `locations.region_id` nullable; regions are new master data drawn fresh. Locations re-parented via a **bulk form** (validates `region.rayon_id == location.rayon_id`); **region delete sets child `region_id = NULL`** (no cascade).
- **Per-level map styling** (ADR-045) — separate `border_color` / `fill_color` / `border_opacity` (0–1) / `fill_opacity` (0–1) + `marker_icon`/`marker_image_url` (image-only markers; configured `marker_color` removed) on all four tiers, each edited in its own master-data surface incl. a **City styling card** for the single Surabaya row (legacy single `color` kept as fallback; colors `^#[0-9A-Fa-f]{6}$`).
- **Static vs mobile** — a region enables mobile (roaming) subjects geofenced to the region; locations remain static geofences.
- **Polygon boundaries** (ADR-010) — every level carries editable polygons used for geofencing.
- `kecamatans` (read-only reference) and `location-staff-requirements` are **backend-only** (no dedicated UI).

## Implementation
- **API:** [`../../api/contracts.md`](../../api/contracts.md) · errors [`../../api/error-handling.md`](../../api/error-handling.md) (live Swagger `/api/v1/docs`)
- **Database:** [`../../database/schema.md`](../../database/schema.md)
- **Web:** City styling card, Rayons (CRUD), Regions (CRUD + polygon editor), Locations (CRUD + polygon editor + bulk re-parent) — [`../../platforms/web/pages.md`](../../platforms/web/pages.md)
- **Mobile:** location/rayon context in monitoring & field screens — [`../../platforms/mobile/screens.md`](../../platforms/mobile/screens.md)

## Related features
- [monitoring](../monitoring/README.md)
- [users](../users/README.md)
- [plants](../plants/README.md)
- [teams](../teams/README.md)

## Known gap — inactive rayon/kawasan vs monitoring (TODO, revisit)

`is_active` now exists on all four levels (City is implicit; Rayon/Kawasan added 2026-07-15).
Two decisions were deliberately deferred — **revisit during the Phase-5 monitoring revamp**:

1. **Monitoring still shows deactivated rayon/kawasan, on purpose.** Hiding them would also hide the
   **live workers** clocked in under them — a safety-visible change, not a cosmetic one. Web pins this
   with `useRayons(true)` in `components/monitoring/HierarchyFilterPanel.tsx`; the backend monitoring
   services query the `Rayon` repository directly and never call `rayonsService.findAll()`, so the
   new active-only default does not reach them. **If monitoring is ever made to hide inactive
   levels, pair it with the deactivate guard** (below) or a worker can silently vanish from the map.
2. **Scope of "hide it" is master data + schedule only** (per the operator): grids, filters, pickers
   and the schedule forms. Tasks/overtime/plants pickers inherit the active-only default via
   `useRayons()`/`useRegions()` — not separately audited.

**Guard, not cascade** (chosen over hiding children): deactivating is **refused with 409** while a
rayon still has active kawasan/lokasi/petugas (`RAYON_DEACTIVATE_IN_USE`), or a kawasan still has
active lokasi (`REGION_DEACTIVATE_IN_USE`). Nothing is ever hidden implicitly, so there is no
"active lokasi under an inactive rayon" state to reason about. `activate()` is never guarded, and
`findOne()` stays unfiltered — filtering it would 404 the very record being reactivated.

**Open tension — delete guards are asymmetric.** Rayon `remove()` already refuses while lokasi
reference it (`rayons.service.ts`), but Kawasan `remove()` deliberately does **not**: per ADR-045 it
detaches children (`region_id = NULL`) and soft-removes. A delete guard on Kawasan was requested;
it is **not implemented here** because it would contradict ADR-045. Resolve the ADR first if the
guard is wanted — deactivate (guarded) and delete (detaches) intentionally differ today.

## Known gap — "Area" display strings not yet swept (TODO)

The 2026-07-15 sweep fixed the **`/locations` page identity** (title/breadcrumb/nav keys) and the
**`areaType` → `locationType`** relation, but **~70 user-facing strings still read "Area" where they
mean a *Lokasi***. Inventoried, not yet renamed — deferred deliberately, sweep in a later pass:

| Namespace | Count | Examples |
|---|---|---|
| `schedules` | 17 | `modals.areas.title` "Ubah Area" · `modals.areaList.title` "Area Jadwal" · `buttons.areaCount` "{{count}} Area" |
| `admin` | 16 | `users.form.areaAssignment` "Area Penugasan" · `rayons.columnAreas`/`stats.totalAreas` "Jumlah Area" · `rayons.areaDetail.areaName` "Nama Area" |
| `tasks` | 12 | `list.tableHeaderArea` "Area / Rayon" · `form.areaLabel` "Area (Opsional)" · `form.areaPlaceholder` "Pilih Area" |
| `import` | 12 | `kmz.areaTypeLabel` "Tipe Area" · `kmz.columns.name` "Nama Area" · `export.allAreas` "Semua Area" |
| `plants` | 5 | `catalog.description` "…per area" · `areaDetail.notFound` "Area tidak ditemukan" |
| `activities` | 4 | `list.filters.area`/`allAreas` · `list.table.columns.area` |
| `common` | 4 | `entities.area` "Area" · `empty.noAreas` · `stats.totalArea` "Total Area" (renders `total_areas`, a **count** — not m²) |
| `overtime` | 2 | `list.table.columns.area` · `detail.fields.area` |

**Out of scope — do NOT rename:**
- **`monitoring` (49 strings)** — "Batas Area", "Dalam area", "Titik Area", `sidebar.tabAreas` etc. are **correct**: a *monitoring area* legitimately = rayon/kawasan/lokasi. Also gated behind the Phase-5 monitoring revamp. Renaming these is a regression.
- **Area-as-size:** `admin:locations.coverageLabel` "Luas Area", `rayons.stats.totalCoverageArea` "Luas Tutupan", `coverage_area`.
- **Geofence state:** `common:ui.withinArea`/`outsideArea`, `home:statusSection.outsideArea`.
- **Wire contracts:** `area_type` (response field + query param), monitoring `area_ids`, `/areas` import/export entity keys, `AreaType`/`areaTypesService` (= the *LocationType* service/repo, not the relation).

Prose examples (e.g. `tasks:form.titlePlaceholder` "Contoh: Penyiraman Area Timur") read as place names — judgement call, not a mechanical rename.

## Changelog
- 2026-07-20 — **Domain term `rayon` → `district` — Stage 3: mobile + i18n split (COMPLETE) ([ADR-052](../../architecture/decisions/ADR-052-rayon-to-district-rename.md)).** Mobile (React Native) caught up: `Rayon`→`District` types, `rayonsApi.ts`→`districtsApi.ts` / `rayonColors.ts`→`districtColors.ts` / `usePruningRayons`→`usePruningDistricts` (+ file renames), `/rayons`→`/districts` API paths, monitoring scope `'rayon'`→`'district'`, WS `subscribe:district` (clears the last contract-lag surface), constants `RAYON_PALETTE`/`RAYON_FALLBACK`→`DISTRICT_*`. **i18n split:** locale keys `rayon*`→`district*` in both `id`+`en`; **`en` values → "District", `id` values stay "Rayon"**. Kept: role codes/labels, district-name data ("Rayon 1"). Over-swept Indonesian test assertions ("Per district", "di district ini", "area, district") reverted to the rendered id UI. Verified: mobile **tsc 0 · 233 suites/4222 jest · eslint 0 err · i18n parity**. **ADR-052 rollout now COMPLETE across DB · backend · web · mobile;** the whole stack reads `district` (code/DB/English UI) with "Rayon" retained only as the Indonesian UI label.
- 2026-07-20 — **Domain term `rayon` → `district` — Stage 2: web + i18n split ([ADR-052](../../architecture/decisions/ADR-052-rayon-to-district-rename.md)).** Web caught up to the Stage-1 backend: `Rayon`→`District` type, `useRayons`→`useDistricts` + all hooks/vars, API module `api/rayons.ts`→`api/districts.ts`, route folder `/(dashboard)/rayons`→`/districts` (+ nav hrefs), monitoring drill scope/variant `'rayon'`→`'district'`, WS `subscribe:district` (clears the Stage-1 contract lag), wire field `total_rayons`→`total_districts`. **i18n split done together:** locale KEYS renamed `rayon*`→`district*` in both `id`+`en` (parity kept); **`en` VALUES → "District", `id` VALUES stay "Rayon"** (default UI still reads "Rayon"). Kept: role codes/labels (`admin_rayon`/`kepala_rayon`, "Kepala Rayon"), and district-NAME data values ("Rayon Barat 1"). Over-swept Indonesian test assertions (`/batas district/i` etc.) reverted to match the rendered Indonesian UI. Verified: web **tsc 0 · 154 suites/2124 jest · eslint 0 err · i18n parity · `npm run build` green** (`/districts` route). Next: mobile.
- 2026-07-20 — **Domain term `rayon` → `district` — Stage 1: DB + backend ([ADR-052](../../architecture/decisions/ADR-052-rayon-to-district-rename.md)).** The city↔kawasan tier is renamed to **`district`** for DB/code/English-UI; **"Rayon" stays as the Indonesian UI term** (i18n, Stage 4). Physical DB rename via migration `17510000000000` — `rayons` → `districts` + every `rayon_id`/`to_rayon_id` → `district_id`/`to_district_id` across 14 tables (idempotent + reversible); **district *names* are data and stay Indonesian** (`"Rayon Pusat"`, `"Rayon Barat 1"` untouched). Backend swept: entity `Rayon`→`District`, module `rayons/`→`districts/`, route `/rayons`→`/districts`, DTOs (`RayonStatsDto`→`DistrictStatsDto`, …), `StaffingLevel.RAYON`→`.DISTRICT`, monitoring scope `'rayon'`→`'district'`, WS `monitoring:district:{id}`/`subscribe:district`. **Deliberately kept `rayon`:** role codes `admin_rayon`/`kepala_rayon` (frozen JWT/guard contract), DB constraint/index NAMES, frozen migrations, snapshot seed keys + the pruning CSV-import contract (`rayonName`). ⚠️ **Contract lag:** WS event is now `subscribe:district` while web/mobile still emit `subscribe:rayon` until Stages 2–3 (backend-first, self-heals). Verified: be **tsc clean · 149 suites/2459 · eslint 0 err**; app boots on the renamed schema (`GET /districts` → `name:"Rayon Barat 1"`, live `users.district_id` queries). Stages next: web · mobile · i18n split.
- 2026-07-20 — **Monitoring drill-level `'area'` → `'location'` (partly supersedes the "kept in monitoring" note below).** The earlier decision retained ALL `area` tokens in monitoring. That's now split: the **drill-level SCOPE / aggregate-node `type` / marker `variant` value** `'area'` was renamed to `'location'` across backend + web + mobile (see `features/monitoring/README.md`), because it names the **lokasi entity** and read confusingly as `area` next to `location` everywhere else. **Still kept** (the genuine "a monitoring area = any level" / wire-contract tokens): `AreaBoundaryDto`, the boundaries payload `.areas[]`, the **`?level=area`** boundaries param, `area_ids`, `area_type` (= LocationType), the "Batas Area"/"Dalam area"/"Titik Area" display strings, the sidebar `area` tab, and the `uts.area` TypeORM relation. Net: `scope`/`type`/`variant` = `location`; the boundary tier param stays `level=area`.
- 2026-07-18 — **Unify markers on icon + color — Phase 4: remove marker_image_url ([ADR-051](../../architecture/decisions/ADR-051-unified-icon-color-markers.md)).** Dropped `marker_image_url` column + system entirely: backend entities (rayons, regions, locations, roles, team-categories) have the column removed via migration `17508000000000`; all DTOs/services cleaned; web types/API models/forms updated; dead components deleted (`MarkerImagePicker.tsx`, `markerPresets.ts`); `markerDefaults.ts` reduced to type exports only; marker asset files removed (`9/9 pin-*.svg`). Backend tsc/jest + web tsc/jest + i18n all green; grep confirms zero trace of `marker_image_url` outside migrations + specs. Both workspaces fully unified on glyph+color system.
- 2026-07-17 — **Unify markers on icon + color — Phase 3: editors + previews ([ADR-051](../../architecture/decisions/ADR-051-unified-icon-color-markers.md)).** Every marker surface now reads glyph + color, not `marker_image_url`: the `CoordinateLink` preview modal + `GoogleBoundaryEditor` center pin render the unified `pinMarker(glyph, border_color)`; the rayon/kawasan/lokasi forms use the **glyph picker** (`MarkerIconPicker` in `MapStyleFields`) instead of the image picker; the role editor drops its redundant image picker (keeps the glyph picker from #312); the team form drops the image picker (teams are color-only bubbles). `marker_image_url` is now unread everywhere — the field + `MarkerImagePicker`/presets are removed in Phase 4. web maps/forms tests pass (53).
- 2026-07-17 — **Unify markers on icon + color — Phase 1: migration ([ADR-051](../../architecture/decisions/ADR-051-unified-icon-color-markers.md)).** Decision: retire the `marker_image_url` image system and render **one glyph + color pin everywhere** (glyph = `marker_icon`, color = the area's `border_color`, + count/status ring), so the map, editors, and worker pins share a single composable representation. Migration `MarkerIconFromImagePreset` backfills each area's `marker_icon` from its `marker_image_url` **preset** (`pin-purple` → `star`, `pin-teal` → `droplets`, `pin-orange` → `building`, …); uploaded images (e.g. tes rayon) have no glyph equivalent and fall back to the per-kind default. `marker_image_url` is left intact until the web reads `marker_icon` (Phase 2) and is dropped in the cleanup phase. Behaviour-preserving on its own (nothing renders `marker_icon` for areas yet). Verified: Barat 1 → `star`, Utara → `droplets`.
- 2026-07-17 — **Extend the styled boundary + marker preview to kawasan + lokasi.** The rayon preview (both the *Koordinat*-column `CoordinateLink` modal and the live *Batas & Titik Pusat* editor) now also applies to **kawasan** and **lokasi**: `regions/page.tsx` + `locations/page.tsx` grids pass each row's boundary/colors/marker + `entityKind` (`region`→yellow, `area`→green default pins); `RegionForm` + `LocationForm` feed `GoogleBoundaryEditor` the live watched style; and the read-only lokasi detail (`locations/[id]`) draws its boundary in the configured colors + marker. Verified live (Kawasan preview: green boundary/fill + yellow default pin, framed). web maps/forms tests pass (22).
- 2026-07-17 — **Live style preview in the rayon boundary editor + opacity readout fix.** Two form fixes (ADR-045). **(a)** `MapStyleFields` opacity sliders showed a bare **"—"** whenever `border_opacity`/`fill_opacity` were unset (the common case) even though the slider sat at a default position — now the label shows the **effective opacity as a percentage** (`border` → 100%, `fill` → 25% defaults, matching how the map renders), so the readout always tracks the slider. **(b)** `GoogleBoundaryEditor` gained optional `strokeColor`/`strokeOpacity`/`fillColor`/`fillOpacity`/`markerImageUrl` props; **`RayonForm` feeds it the live watched style**, so the *Batas & Titik Pusat* map now draws the boundary in the configured **border + fill colors** and shows the **configured/default marker** at the center (rayon → orange pin) instead of the neutral black outline + red pin — updating as you edit, and in the read-only *Lihat* view. Backward-compatible: region/location editors keep the default styling until they pass the props. web maps/forms tests pass (19).
- 2026-07-17 — **Rayon coordinate preview → styled boundary + marker preview.** The `CoordinateLink` map modal (opened from the master-data grid's *Koordinat* column) previously dropped a bare Google pin at `center_lat/lng`. `MapDisplayModal` now optionally takes the entity's `boundary_polygon` + `border_color`/`fill_color`/`fill_opacity` + `marker_image_url`/`entityKind`: it draws the **boundary polygon in the configured colors**, **frames the map to it** (`fitBounds`), and renders the entity's **configured marker image — or its per-kind system default** (`entityMarkerDefault`; rayon → orange building pin) — via an `AdvancedMarker` DOM `<img>` instead of the generic pin. Wired for **rayon first** (`rayons/page.tsx` passes the row's boundary/colors/marker + name); `region`/`location` grids can adopt the same props (kawasan → yellow, lokasi → green defaults). Backward-compatible — callers that pass none still get the plain pin. Verified live (Rayon Pusat: green boundary + tint + orange marker, framed). web maps tests +4.
- 2026-07-16 — **Phase 5.2 — placeholder kawasan geometry (demo seeder only).** ADR-045 gave kawasan per-level styling but **never a shape**: **0 of 129** carried a `boundary_polygon` *or* a `center_lat/lng`, while all 129 had colours. So ADR-046's region tier had literally nowhere to draw a bubble and mobile crews had nothing to be geofenced against — the whole Kawasan tier of Phase 5 was blocked on missing geometry, not missing code. New `seedRegionGeometry` (**demo only**; runs after `seedRegions` because it derives from the lokasi that step re-parents) fills every kawasan that is still empty. **These are dummies, not data** — the client is supplying real KMZ (or drawing them) in parallel, and import via the existing `ImportBoundaryButton`/`GoogleBoundaryEditor` replaces them; the seeder only ever fills `boundary_polygon IS NULL`, so a real boundary is never clobbered. Three shapes, because the data is uneven: **86** kawasan have ≥3 lokasi → **convex hull** of their pins (monotone chain), expanded 25% so members sit inside; **29** have 1–2 → their **bounding box + 150 m**; **14 have no lokasi at all** → a box offset from the rayon centre, fanned by index so they don't stack — purely arbitrary, and **logged as such**. **Caught by verifying rather than assuming:** the first cut put a fixed 300 m box on the *centroid* of the 1–2-lokasi kawasan, which left **10 lokasi outside their own kawasan** (two pins further apart than the box). Bounding the pins instead → **590/590 member lokasi now fall inside their kawasan**, and 115/115 centres sit within their members' bounding box. Verified: kawasan **129/129** polygon + centre · be 148 suites/**2403** · tsc/eslint clean.
- 2026-07-16 — **Phase 5.1 — `locations.radius_meters` retired; geofencing is coordinate + boundary only.** It was never data: **every** row carried the same `100`, hardcoded at `import.service.ts`. It only mattered as the **fallback** in `GpsUtil.isWithinAreaBoundary` for the **5** lokasi with no polygon (all real parks in Rayon Taman Aktif — Kunang2, Kombes, Korea, Pandugo, Wirasurya). **The trap:** that helper's last branch is *"no boundary defined → allow"* — it **fails OPEN**. Dropping the column naively wouldn't have un-geofenced those parks loudly; it would have made them **always-inside and silent**. So migration `17504000000000` **converts each radius into a real circle polygon first**, then drops the column — those lokasi keep the exact geofence they had, expressed as geometry (`cos(lat)`-corrected, so it's a circle not an ellipse; **live-verified at 100.0 m on both axes**). **Found while running it:** re-seeding brought the 5 back polygon-less — the snapshot has no geometry for them, and a completed migration never re-runs, so a fresh DB would have had **5 silently ungeofenced parks**. The seeder now derives the same ring from the snapshot's `radius_meters` (retired as a *column*, kept as *snapshot input*). **Live-verified: 953/953 lokasi now carry a boundary** (was 948) and the column is gone. **Cross-platform sweep** (~80 files): backend entity/DTOs/`/me` contract/import/export/seeds; web types, the LocationForm radius input + its zod rule, the grid column and detail row; **mobile — the part that mattered**: `ShiftDetailModal` computed `isInside: distance <= (radius_meters ?? 100)` as its **clock-in indicator**, so that `?? 100` would have invented a geofence the server no longer shares — it now calls the shared `isWithinAreaBoundary`, which mirrors the backend. Circle overlays in `LocationMapModal`/`BoundaryOverlay` draw **nothing** without a ring rather than a phantom circle; `getAreaCircles` deleted (radius-only, and **nothing rendered it** — dead before this change). 20 orphaned i18n keys removed across both locales on both platforms — `i18n:check` validates parity, not usage, so it can't see them. Tests that encoded the retired fallback were rewritten, not deleted; the clock-in fixtures gained real polygons (an "outside area" warning is undetectable without one). ⚠️ **Installed-APK contract change**, alongside `areaType`→`locationType`. Verified: be 148 suites/**2402** · web 147/**2078** · mobile 233/**4228** · build green · tsc/eslint/i18n clean on all three.

- 2026-07-16 — **Location-type catalog revised; `category` confirmed decorative.** `location_types` is a **TABLE, not an enum** — types are data and the backend already exposes full CRUD (`USER_MANAGERS`-gated), so operators change them via the API; **no management page** was added (operator's call — not worth it for a 4-row catalog). Catalog now: **Taman · Trotoar · Jalan · Pulau Jalan**. `Jalanan` → **`Jalan`** (display `name` only — `code` stays `street`, so all **841** lokasi keep resolving through an unchanged FK); **`traffic_island` / "Pulau Jalan"** added (PASSIVE, matching every other road-side type); **`mini_garden` / "Taman Mini"** retired (**0 lokasi**). Migration `17503000000000` moves an existing DB (staging) onto the same baseline the seeder writes for a fresh one — the two must not drift. **Retirement is guarded, never forced:** if any lokasi still points at a type it is **soft-deleted** rather than removed, so those rows keep resolving and an operator re-points them deliberately — a migration must not orphan live rows. `down()` restores the prior catalog from either branch. **Live-verified up → down → up on the seeded DB; 953 lokasi resolving throughout.** Also fixed: the seeder's `ON CONFLICT (code) DO NOTHING` could only ever INSERT, so a **rename silently no-opped** on any DB already holding the row — now `DO UPDATE`, making the seeder authoritative about its own baseline. **`category` (ACTIVE/PASSIVE) is decorative and stays so** (operator's call): traced every consumer — seeded, carried on two monitoring DTOs as `area_type_category`, colours one badge on the locations grid/detail. **Nothing branches on it** — not monitoring, staffing, scheduling, geofencing or reporting. (Latent, harmless while unused: `monitoring-stats.service.ts` falls back to `|| 'active'` **lowercase** while the column stores `'ACTIVE'`, so a type-less lokasi emits a value the web's `=== 'ACTIVE'` check misses.)

- 2026-07-15 — **Rayon + Kawasan gain `is_active` (Nonaktif), closing the master-data gap.** Lokasi and Kategori Tim already had a reversible soft-retire; rayon/kawasan had nothing, so the four levels behaved differently for no reason. Migration `17502000000000` adds `is_active boolean NOT NULL DEFAULT true` to both and backfills — behaviour-preserving on its own (verified on the live local DB: **9/9 rayons + 130/130 kawasan** active, `down()` drops both cleanly). `findAll` is **active-only by default** on both (it previously had *no* filter at all), with `include_inactive` for the management grids; new `PATCH :id/{deactivate,activate}`. DTOs deliberately untouched — `is_active` is settable only via those endpoints, never the form, mirroring lokasi. **Guard, not cascade** (operator's call): deactivation is **refused with 409** while a rayon still has active kawasan/lokasi/petugas (`RAYON_DEACTIVATE_IN_USE`, reports *every* blocker, not just the first) or a kawasan still has active lokasi (`REGION_DEACTIVATE_IN_USE`) — so nothing is hidden implicitly and there is no "active lokasi under an inactive rayon" state. Codes added to `ApiErrorCode` + both platforms' `errors` namespace so the toast localizes by **code** (the API stays English-canonical) rather than matching message text — the fragile pattern the rayon *delete* path still uses. Web: `StatusPill` + enum Aktif/Nonaktif filter + `Power` rowAction on both grids, reusing `admin:shared.*`. **Caught while wiring it:** flipping the `useRayons()` default silently changed non-picker callers — `HierarchyFilterPanel` (monitoring) and the id→name resolvers (`ScheduleDetailModal`, `ScheduleFilterChips`, `UserAreasSheet`) would have lost deactivated records; those now pass `includeInactive` explicitly. See "Known gap — inactive rayon/kawasan vs monitoring" above. Verified: be 148 suites/**2387** (+15) · web 143/**1967** · tsc/eslint/i18n clean.
- 2026-07-15 — **Koordinat + Batas Wilayah standardized across the rayon / kawasan / lokasi grids.** The three grids disagreed on both columns. **Koordinat** is now identical everywhere — **sort off, filter off** (raw lat/lng orders and filters meaninglessly), with an `accessorFn` returning `"lat, lng"` purely so global search can match a pasted coordinate. Fixes found on the way: **lokasi's Koordinat was still sortable** (it had `enableColumnFilter: false` but no `enableSorting: false`), **rayon's accessor returned a hardcoded `'Ada'`/`'—'`** (an untranslated literal, and inconsistent with lokasi's coordinate string), and **kawasan had no `accessorFn` at all**, so coordinate search silently never matched there. **Batas Wilayah** (`enum` filter + `StatusPill` Ada/Tidak ada) was lokasi-only and is now on **all three** — all three entities already carry `boundary_polygon`, so this is display-only. Labels moved to `admin:shared.{columnCoordinates,columnBoundary,boundaryYes,boundaryNo}` (the rayon/kawasan grids had been borrowing `admin:locations.columnCoordinates`, which read oddly for a rayon); the now-unused `admin:locations.*` duplicates were dropped. Verified: web tsc/eslint/i18n + full jest (1967).
- 2026-07-15 — **`/locations` stopped calling itself "Area".** The page title + breadcrumb still read **"DATA MASTER · AREA"** over a body that said "953 lokasi": `usePageNavigation` and `layout.tsx` were pointing at stale i18n keys (`pageTitle.areas`, `pageMetadata('areas')`) — the *values* were the only thing ever updated, so the sidebar already said "Lokasi" while the page header didn't. Renamed the keys themselves: `pageTitle.areas` → `.locations`, `pageTitle.analyticsAreas` → `.analyticsLocations`, `common:nav.areas` → `.locations`, `PAGE_METADATA.areas` → `.locations` (+ "Manajemen Area" → "Manajemen Lokasi"), and the sidebar nav item id `areas` → `locations`. **Batas Wilayah** now renders the shared `StatusPill` (Ada = `tone="ok"`, Tidak ada = `tone="neutral"`) instead of bare text, matching the Status column beside it. Deliberately **not** renamed (real contracts, not leftovers): the `area_type` query param, monitoring `area_ids`, `coverage_area`, and the `/areas` import/export entity keys.
- 2026-07-15 — **Renamed Location entity relation `areaType` → `locationType`.** Updated the TypeORM relation property and all eager-load paths in API responses (Location, Shift, Report). Db column `location_type_id` is unchanged — no migration. **Silent data bug fixed:** web's `Location` type and `/locations` grid already read `locationType`, so the mismatch meant **Tipe column rendered "—" for every row**. Mobile (ClockInOutScreen, ShiftHistoryScreen, AssignedAreaCard) updated in the same change; **APKs already in the field show "N/A" for area type until updated**. The separate snake_case `area_type` response field + query param, monitoring `area_ids`, `coverage_area` — all unchanged.
- 2026-07-15 — **Save button no longer gated on geometry (rayon/kawasan/lokasi).** The three CRUD forms disabled the Save button unless a boundary or center pin was present (`disabled={!hasGeometry}`) — so a rayon/kawasan with no geometry (kawasan boundaries are optional per ADR-045; test rayons had none) could never be saved. The button is now gated only by the zod required-field validation (name, rayon, etc.) + the loading state; boundary/pin are optional. Removed the `hasGeometry`/`onValidityChange` plumbing + the `geometryRequired` hints from all three form/modal pairs. Lokasi still *requires* a point (gps) — its previous **silent** early-return on no pin/boundary is now a **visible** error toast (`validation:locationPointRequired`). Verified: web tsc/eslint/i18n + full jest (1855).
- 2026-07-15 — **Fix: editing a rayon/kawasan/lokasi that has opacity set silently wouldn't save.** The map-style `border_opacity`/`fill_opacity` are DB `decimal` columns, which TypeORM returns as **strings** (`"0.80"`). Since #246 added them to the form schema as `z.number()`, an unchanged edit loaded the string and **silently failed zod validation** (RHF sets a field error but logs nothing) → the submit button did nothing, no PATCH, no console error → "stuck". Records with null opacity were unaffected. Fix: coerce the opacities to `Number` on load in all three forms (mirroring how `center_lat`/`center_lng` were already coerced). Regression test: an edit with string opacities now submits with numeric opacity. Verified: web tsc/eslint + form tests (17).
- 2026-07-15 — **Renamed the `admin:areas.*` i18n namespace → `admin:locations.*`** (code-level cleanup of the retired "area" management term; 136 call sites across 11 files + the JSON block in both locales). **No DB rename needed** — the geography model was already renamed to `locations` under ADR-010 (every current entity uses `location_id`; there is no `area_id` column). The remaining code-level `area` tokens are intentional and stay: `area_type` (snake_case response field + query param = *LocationType*), `coverage_area`, monitoring `area_ids` (a **monitoring area** = rayon/kawasan/lokasi), `AreaBoundaryDto` (monitoring), and frozen historical migrations. (Lone parked exception: `seed_transaction.to_area_id` in the parked plant-seeds feature.) Verified: web tsc/eslint/i18n + full jest (1856).
- 2026-07-15 — **Standardized "Warna" colour column + removed "Kelola Area".** (1) A shared `MapStyleSwatch` + `mapStyleColorColumn` (`components/ui`) now render the colour column identically across the **rayon / kawasan / lokasi** grids: a swatch that reflects border+fill **at their opacities**, plus a compact `#hex · NN%` readout for both, titled `common:color` ("Warna"/"Color"), **sort + filter disabled** (a hex-text filter was useless). Roles show their monitoring `marker_color` as a small chip on the list card. (2) Removed the **"Kelola Area"** action from the Kawasan grid + its `AssignAreasModal` and dead `admin:regions.assignAreas.*` / `columnStyle` keys — the Kawasan↔Lokasi assignment now lives **only** in the Lokasi form (`region_id`, fixed in #247). "Area" as a management term is retired; it now means only a *monitoring area* (rayon/kawasan/lokasi). Verified: web tsc/eslint/i18n + full jest (1856).
- 2026-07-15 — **Fix: Rayon/Lokasi map-style colours were silently dropped on save.** `RayonForm` + `LocationForm` set the map-style fields (`border_color`/`fill_color`/opacities/marker) via `MapStyleFields` + `setValue`, but those keys **weren't declared in the zod schema**, so `zodResolver` **stripped them from the submitted data** — the form showed the colour but the PATCH sent `null` (backend + DB were fine; verified via direct API PATCH). Added the six map-style fields to both schemas (Kawasan/`RegionForm` already had them, which is why only Rayon/Lokasi were affected). Regression test asserts the colours survive to the submit payload. Verified: web tsc/eslint + RayonForm jest.
- 2026-07-15 — **Retired the legacy `rayons.color` column.** ADR-045's per-level styling (`border_color`/`fill_color`) is now the *sole* boundary-colour source — the leftover single `color` column (and its form mirror + grid fallback) is gone, removing the split-source confusion. Migration `17501000000000` **backfills `border_color`/`fill_color` from `color`** (so existing rayons keep their tint) then **drops the column**; monitoring boundaries now **derive their map colour from `border_color ?? fill_color`** (the `/monitoring/boundaries` `color` field stays as a derived output — no web-map change). Seed writes border/fill from the snapshot's colour. Verified on the live local DB: 68 migrations, 7 coloured rayons backfilled + column dropped; be monitoring/rayon suites (67) + web rayon/monitoring suites (390) green.
- 2026-07-15 — **Master-data CRUD sync (Rayon/Kawasan/Lokasi).** UX + correctness pass on the three geography master forms/grids: (1) Rayon **`staffing_level`** select reordered to the natural hierarchy *Seluruh Rayon → Per Kawasan → Per Lokasi* and **no longer preselects on create** (placeholder + required-on-submit). (2) **Color "not saved" fix** — the Rayon grid read the legacy single **`color`** column while the form edits per-level **`border_color`/`fill_color`** (ADR-045), so a fill-only edit looked unsaved; the grid swatch now renders border+fill (with `color` fallback for un-restyled rayons). *(The legacy `color` column is retained deliberately — the monitoring map + boundaries API still consume it and the seed only populates it; migrating monitoring off `color` and dropping the column is a Phase-5 follow-up.)* (3) **"Gaya Peta" → "Tampilan Peta"** (Map Display). (4) **Field↔grid↔filter sync:** Rayon grid gains a **Tingkat Kebutuhan** column + enum filter; Kawasan grid gains a rayon enum filter + coordinates/description columns and a border+fill swatch; Lokasi grid gains a style swatch and **`radius_meters` is now editable in the Lokasi form** (was grid-only). (5) **Dead code removed:** unused `mapStyle.markerIcon/markerIconPlaceholder/markerColor` i18n keys (id/en). Also added Radix pointer polyfills to web `jest.setup.ts` so Select options can be driven in tests. Verified: web tsc/eslint/i18n + full jest (1851) green.
- 2026-07-15 — **Dress-rehearsal fixes (full migration chain green from scratch).** Running the whole 67-migration chain on an empty DB (Step 8) caught two deploy blockers, now fixed: (1) the **data-adoption migrations `17496`/`17500`** (kawasan + staffing) inserted rows referencing rayons/regions that don't exist on a fresh migration-built DB (the seeder runs *after* migrations) → they now **skip on an empty DB** (guard on `rayons`/`regions` count; the seeder handles fresh). (2) The pre-revamp **`uq_area_staff_requirements`** unique (location-only) is incompatible with the polymorphic model (region/rayon rows, NULL `location_id`) — `17499` now **drops it** (sync-built DBs never had it). Also **retired the superseded `area-staff-requirement` seeder** (ad-hoc Bungkul/Buk Tong + 332 rows) — the workbook staffing (`seedStaffingRequirements`) is the sole source; `17500` is authoritative on live adoption (clears prior auto-seeded staffing before inserting). Verified: fresh DB → 67 migrations → seed → **195 staffing rows, 0 duplicates**; 504 be tests green.
- 2026-07-15 — **Web understaffing + editor by subject (3b-2).**
- 2026-07-15 — **Web understaffing + editor by subject (3b-2).** The Jadwal day board now flags understaffing at the level each rayon uses: **kawasan rows** show satgas+linmas (across the kawasan's placement + all its locations) vs the kawasan KEBUTUHAN for grouped rayons; **Taman Aktif parks** keep location-level pills; locations under a kawasan show counts only. `requirementTotalMap` keys became subject-prefixed (`loc:`/`reg:`/`ray:`) — a target renders wherever its subject appears. The gear opens the shared **`CapacityModal`** for the subject: kawasan gear → region requirements, park gear → location requirements. Backend gained region/rayon write+read endpoints (`/regions/:id/staff-requirements`, `/rayons/:id/…`) + `bulkSetForRegion`/`bulkSetForRayon`; monitoring understaffing still location-only (unchanged). Tests: subject-key map + backend service; tsc/eslint/i18n green.
- 2026-07-15 — **Polymorphic staffing requirements + seeded from the workbook (3b-1).** `location_staff_requirements` is now polymorphic — a target attaches to a **location**, **region (kawasan)**, or **rayon** (nullable `region_id`/`rayon_id` added; `location_id` made nullable; CHECK "at most one subject"; migration `17499000000000`, additive). Seeded the real **KEBUTUHAN** from the client workbook (`data/staffing.snapshot.json`): **195 satgas/WEEKDAY rows** — 147 kawasan-level (the 7 grouped rayons) + 48 location-level (Taman Aktif parks), keyed to each rayon's `staffing_level`. Runs for demo + staging; non-destructive migration `17500000000000` lets live staging adopt them (idempotent, `ON CONFLICT DO NOTHING`). Monitoring's understaffing still reads **location-level** only (region/rayon-level is consumed by the day board + Phase-5 map — guarded, behaviour unchanged). Linmas + weekend/holiday targets are added via the UI later (the sheet is satgas-only). **Next (3b-2):** day-board understaffing by subject + the shared staffing editor placed by `staffing_level`.
- 2026-07-14 — **Rayon `staffing_level` (polymorphic staffing prep).** New `rayons.staffing_level` enum (`region` | `location` | `rayon`, default `region`) declares the tier a rayon's staffing requirements attach to — the 7 grouped rayons keep `region` (kawasan-level), **Taman Aktif** = `location` (per-park, no kawasan). Editable in the rayon CRUD form ("Tingkat Kebutuhan Petugas"). Additive migration `17497000000000` (defaults all to `region`, flips Taman Aktif by its stable id); seeded from `rayons.snapshot.json`. The consumer (polymorphic `staff_requirements` + understaffing by subject) lands next. Confirms the **optional-Kawasan hierarchy**: `Rayon▸Kawasan▸Lokasi` and `Rayon▸Lokasi` are both first-class (`region_id` nullable, ADR-045).
- 2026-07-14 — **Areas re-parented under kawasan + staging migration.** Areas are now grouped under their kawasan by matching the workbook's per-kawasan RTH lists (column K, each "Kawasan…" header followed by its RTH rows) to staging area names — **590/953 confident matches** (`data/area-region.snapshot.json`); the rest stay unassigned for UI remediation (Taman Aktif's 43 correctly have none). `seedRegions` now runs a bulk re-parent (only where `region_id IS NULL`). New **non-destructive** migration `17496000000000-SeedKawasanAndReparentAreas` seeds the 129 kawasan + sets `region_id` on the live staging DB without a full reseed — idempotent, never clobbers a manual UI re-parent, clean `down()` (verified up/×2/manual-preserve/down against a local DB).
- 2026-07-14 — **Areas seeded from live staging (KMZ retired).** All ~953 locations now come from `data/areas.snapshot.json` (pulled live from staging, client-validated) for **both demo + staging** — replacing the KMZ-derived geometry and the synthetic "Darmo" demo playground. The demo scenario (user/user-area/shift/overtime/staff-req/plant seeders) was re-pointed onto **real** anchor areas (5 Rayon Pusat locations + real `TAMAN BUK TONG`, Bungkul, Taman Flora — see `lib/ids.ts`). Retired: `kmz-locations.ts`, `data/locations-kmz.generated.json`, `data/locations-taman-aktif.csv`, `scripts/extract-kmz-seed-data.js` + its `seed:extract-kmz` npm script. Areas carry no `region_id` yet (re-parenting under kawasan is the next step).
- 2026-07-14 — **Geography data corrected from live staging.** Rayons are now seeded from `data/rayons.snapshot.json` — the 8 rows pulled live from staging (client-validated names/descriptions/colours/centres/boundaries), replacing `data/rayons.csv` + KMZ-derived boundaries. The client had swapped **Barat 1 ↔ Barat 2** and reorganised the Barat/Timur kecamatan lists; `rayon_code→id` is kept fixed so membership grouping is unchanged (a code no longer necessarily matches its rayon's display name — codes are internal keys only). **Kawasan seeded from the client workbook**: 129 regions from column K ("NAMA RTH", cells prefixed "Kawasan…") of each rayon tab, grouped under the rayon that tab belongs to, mapped by **display name** → staging rayon id (`data/kawasan.snapshot.json`, `seedRegions`, now run for **both demo + staging**). Names + parent rayon only — boundaries drawn fresh in the UI; areas not yet re-parented. Taman Aktif has no kawasan (park-organised).
- 2026-07-12 — **Phase 2 review hardening.** Region **cannot move to another rayon while it has assigned areas** (would break the `region.rayon_id == location.rayon_id` invariant; detach/reassign first). Region + rayon **boundary writes are now GeoJSON-validated** (same rules as location boundaries; simple `Polygon` only — MultiPolygon/self-intersection not modelled by the validator yet). `GET /regions` is **rayon-scoped** for district-scope callers (kepala_rayon/admin_rayon/korlap see only their rayon, mirroring the locations list). Web: region area-assignment now invalidates the `locations` query cache (was a dead `['areas']` key — the locations table showed stale `region_id` until reload). **Deferred (Phase 5):** the monitoring `boundaries` endpoint still returns the legacy `color` field only; the full per-level border/fill/opacity payload ships with the monitoring revamp.
- 2026-07-12 — **Phase 0–3 verification pass.** ADR-045 + this spec corrected to the image-only marker model (`marker_image_url`, no configured `marker_color`); ADR-045's monitoring region-tier claim marked **deferred to Phase 5** (endpoints still expose rayon/location tiers only).
- 2026-07-12 — **Area→Location terminology sweep.** Renamed entities, tables, columns, and routes: `areas` → `locations`, `area-types` → `location-types`, `area-staff-requirements` → `location-staff-requirements`. Keep: "monitoring area" (generic), `area_name`, `is_within_area`, `/me assigned_area`, route `/areas/:locationId/staff-requirements`.
- 2026-07-12 — **Rayon form colour cleanup.** Removed the duplicate legacy single-colour "Warna" field; map styling now has only **Warna Batas** (border) + **Warna Isi** (fill). **Fill is optional** — a toggle (off = no fill / transparent, `fill_color = null`) with a one-click **"Sama dengan warna batas"**. The legacy `color` column now mirrors `border_color` on save (kept for any remaining consumers). Shared `MapStyleFields` change → applies to rayon/region/area; `ColorField` label made optional.
- 2026-07-11 — Kawasan **area re-parenting UI**: "Assign Areas" action + `AssignAreasModal` (multi-select, same-rayon only). Backend `assignAreas` now REPLACE semantics (un-parents deselected areas; empty clears). Users **Shift column removed** (shift comes from schedules).
- 2026-07-11 — Removed rayon/region/area `marker_color` (image-only markers); per-kind default marker; wired `marker_image_url` into the geo forms. Migration `17491900000000`.
- 2026-07-11 — Marker-as-image: `marker_image_url` on rayons/regions/areas via `MapStyleDto` + `MarkerImagePicker` (preseeded gallery + custom upload) in `MapStyleFields`. Migration `17491800000000`.
- 2026-07-10 — Phase 2 landed: `regions` module (CRUD, `region:*`-gated) + migration; per-level styling (border/fill color+opacity, marker) on rayons/regions/areas; `areas.region_id` (re-parent, rayon-matched) + `users.region_id`; web `/regions` (Kawasan) page + `MapStyleFields` on rayon/area/region forms + area→region select. Verified live. City-level styling + monitoring region-tier drill deferred to Phase 5.
- 2026-07-10 — 4-level hierarchy (Region/Kawasan) + per-level map styling planned (ADR-045).
- 2026-07-10 — Spec created in product-docs restructure. History: [`../../history/CHANGELOG.md`](../../history/CHANGELOG.md).
