# Scheduling

**Status:** ✅ Active (ADR-047 revamp landed — Phase 4) · **Backend:** `shifts`, `shift-definitions`, `schedules` (incl. `schedule_events`), `special-day-overrides`, `service-capacity` · **Key ADRs:** ADR-047 (rule + occurrences), ADR-013 (multi-location), ADR-035 (service capacity)

## Overview
"Jadwal" is rule-driven (ADR-047): a **`ScheduleEvent`** holds recurrence, shift, scope (static location / mobile region), and target (individual or team); a **materializer** expands active events into the per-day `schedules`/`schedule_locations` roster over a rolling horizon, so monitoring reads are unchanged. The web page is a **calendar** (month/week/day) with the datatable as a secondary view. Special-day overrides adjust the roster for holidays; service-capacity models rayon × ISO-week × service-type throughput.

## Key decisions
- **Rule + occurrences** (ADR-047) — `schedule_events` (+ `schedule_event_members`) is the rule layer; `schedule-materializer.service` writes occurrences (`schedules.schedule_event_id` set; NULL = manual/ad-hoc). The **standing-template generator is retired** — migration `17492700000000` converted each active satgas/linmas template (shift + primary location) into an open-ended daily event; `POST /schedules/generate` now materializes events for the date.
- **Rolling horizon** — cron (00:15 + 17:00 WIB + boot self-heal) materializes today + N days (`schedule.materialization_days` system setting, default 30); create/edit materializes in-horizon immediately.
- **Time-based overlap guard** — roster uniqueness is `(user, date, shift)`; a user may hold multiple non-overlapping shifts a day. True time-window overlaps are rejected (shift-3 `crosses_midnight` honored; touching windows allowed). Team fan-out reports conflicted members per-member (`materialization.skipped[]`) and schedules the rest.
- **Calendar edit semantics** — *this occurrence* edits the roster row directly and marks it `is_detached` (never regenerated); *this-and-future* splits the series (old event gets `end_date`, a new event continues); *series* re-materializes future non-detached rows. Deleted occurrences leave soft-delete tombstones the generator never resurrects. Past days are never rewritten.
- **Occurrences carry `team_id` + `region_id`** so Phase 5 monitoring renders team bubbles / mobile geofences without another migration.
- **Multi-location** (ADR-013) — a roster row can span multiple locations (`schedule_locations`); event-generated static occurrences carry the event's single location.
- **Service capacity** (ADR-035) — generic `service_capacity` model (rayon × ISO-week × service_type); web UI is the rayon capacity weekly grid (`rayons/[id]/capacity`).
- `special-day-overrides` — date · `WEEKEND|HOLIDAY|SPECIAL` · name. UI: **Hari Libur** on the Jadwal toolbar (`HolidayManagerModal`, `schedule:create`-gated). Its *only* effect is selecting which `day_type` capacity row applies — it does **not** skip roster generation (the `schedules` module has no holiday awareness). `SPECIAL` resolves identically to `HOLIDAY` and is **no longer offered** in the picker (readable, not creatable). Day type resolves in **WIB** on both sides: backend `DayTypeService` (via `TimezoneUtil`), web `resolveDayType(iso, overrides)`.

## Revamp notes (post-UAT) — ADR-047/048 (landed, Phase 4)
"Jadwal" names the whole process, not one entity. Design (all implemented; see Key decisions):
- **Rule + occurrences** — a `ScheduleEvent` holds recurrence (`none | daily | every_n_days | weekly | specific_dates`), shift, and scope; a generator materializes per-day, per-member **occurrences** into the existing `schedules`/`schedule_locations` roster so monitoring reads are unchanged.
- **Calendar UI** — day/week/month (Google-Calendar-style) for create/edit; datatable becomes secondary.
- **Individual + team** — team events name a PIC + invited members (korlap/satgas/linmas), fanned out to member occurrences ([teams](../teams/README.md)).
- **Static vs mobile scope** — `location_id` (static) or `region_id` (mobile, e.g. penyiraman).
- **Only satgas/linmas** occurrences feed understaffing; korlap optional (never auto-materialized); management/kepala_rayon/admin_rayon need no schedule.
- **Multiple shifts/day allowed** — a user may hold non-overlapping shifts (e.g. shift 1 + shift 2). The **overlap guard is time-based** (rejects true time-window overlaps, honors shift-3 midnight crossing), not one-per-day.
- **Rolling horizon** — a daily cron materializes each active event today + N days forward (`schedule.materialization_days`, default 30); event create/edit materializes in-horizon immediately (no blind full-calendar generation). Edits offer this / this-and-future / series scopes.
- **Occurrence link** — `schedules.schedule_event_id` (nullable FK): set = rule-generated, NULL = manual/ad-hoc.

## Implementation
- **API:** [`../../api/contracts.md`](../../api/contracts.md) · errors [`../../api/error-handling.md`](../../api/error-handling.md) (live Swagger `/api/v1/docs`)
- **Database:** [`../../database/schema.md`](../../database/schema.md)
- **Web:** Schedules page (roster grid); rayon capacity weekly grid (`rayons/[id]/capacity`) — [`../../platforms/web/pages.md`](../../platforms/web/pages.md)
- **Mobile:** My Schedule, Shift History — [`../../platforms/mobile/screens.md`](../../platforms/mobile/screens.md)

## Related features
- [geography](../geography/README.md)
- [attendance](../attendance/README.md)
- [overtime](../overtime/README.md)
- [teams](../teams/README.md)
- [monitoring](../monitoring/README.md)

## Changelog
- 2026-07-16 — **"Buat Jadwal" preselects nothing, cascades workers by role, and locks what the board already answered.** (1) **Nothing is preselected.** *Jenis* ("Individu"), *Ruang Lingkup* ("Rayon") and *Pengulangan* ("Sekali") arrived pre-filled, so an operator could save **three decisions they never made**. All three now start blank with a placeholder and are **required** — `kind`/`scope`/`recurrence_type` accept `''` as the un-chosen state and the schema rejects it. `initialFormScope` returns `''` with no prefill (was defaulting to `rayon`). Found via the new test: the geography cascade was gated on `formScope !== 'city'`, which is **true for `''`** — so the Rayon field appeared before any scope was picked. (2) **New Peran select cascades Pekerja.** The worker combobox offered every schedulable role at once, so finding one satgas meant reading past linmas and korlap; picking a role now filters it (`AsyncUserCombobox` already took `roles`). Not sent to the API — the chosen user carries their own role; switching kind/role clears the stale pick so a hidden field can't ride along. (3) **A prefill from "+ Tugaskan" is a fact, not a default.** Clicking a board cell already answers the shift, the container and (from a role or Tim column) the target kind — leaving them editable invited a silent mismatch between the cell clicked and the schedule saved. Those fields now render **disabled**: `lockShift`/`lockKind`/`lockRole`/`lockScope`/`lockRayon`/`lockRegion`/`lockLocation`, each keyed to what was actually passed, so anything NOT prefilled stays open. A role column's assign pins **kind=individual + that role**; the Tim column's pins **kind=team**. (4) **Ruang Lingkup now comes FIRST, Jenis after it** — the form reads **where → who → when**. Scope decides which geography selects exist and which are required, so it is the question every other answer hangs off; asking "Individu or Tim?" ahead of "where?" put the narrower decision before the one that frames it. (5) The scope option **"Seluruh Surabaya (Tim Patroli)" → "Surabaya"**, matching the board node. One test encoded the old rayon-preselect and was rewritten. Verified: web 143 suites/**2034** (+7), tsc/eslint/i18n clean.
- 2026-07-16 — **Tim is a first-class column, teams count toward staffing, and Surabaya is always on the board.** Three findings, one shared cause — *a container that only renders once it already has content can never be filled*. (1) **Teams were invisible to staffing.** `countable` filtered `!isTeam(o)` and `subtreeRoleCounts` read `byRole`, which teams never enter — so a team of 5 satgas left the pill reading **`0/10` with ten people standing in the kawasan**. A team event fans out to one occurrence **per member**, each carrying that member's real role, so its members *are* satgas/linmas on the ground and must count exactly like individuals. New **`countableByRole`** keeps the two ideas apart: `byRole` is what a column **lists**, `countableByRole` is what it **counts**. Team members stay out of `byRole` — a team is a *combination* of roles and can't be filed under one — but the targets now see them. (2) **The Tim column rendered only when `group.teams.length > 0`** and had no `+ Tugaskan`, so a team had nowhere to be assigned from and the column never appeared. It's now always present like the core roles, with its own assign that opens the event modal on its **team target** (`initialTeam`). Its rows were also **dead buttons** — individuals opened the detail modal, teams did nothing; `BoardTeam` now carries its member `occurrences` so any member resolves the same team event. (3) **The city node had the identical bug**: built only `if (cityOccs.length > 0)`, so a *Seluruh Surabaya* schedule could never be created from the board. It's now always `tree[0]`, renamed **"Surabaya"** (was "Seluruh Rayon / Surabaya"), renders its **shift+role table as its body** (no "Penempatan" wrapper — it has no siblings to distinguish it from), and drops the `0 kawasan · 0 lokasi` pill + `Belum ada kawasan atau lokasi` message, both nonsense for a city-wide sentinel. Tests: 4 encoded the old behaviour (city-conditional node, teams-excluded count, Tim-only-when-populated) and were rewritten; 6 of the new ones confirmed to fail against the merged implementation. Verified: web 143 suites/**2027** (+14), tsc/eslint/i18n clean.
- 2026-07-16 — **Search expansion depth now follows what you named (follow-up to the same-day search entry).** The first cut opened **every container holding a roster**, so searching *Kawasan Ambengan* unfurled all its lokasi — a wall of expanded cards burying the thing asked for. The depth rule is not "where are the rosters" but **what kind of thing was named**: **geography** (rayon/kawasan/lokasi) *is* the destination, so open the chain down to it and **stop** — what's inside is the answer's contents, not more search results (searching a rayon opens the rayon, not its 11 kawasan / 87 lokasi); **subject** (petugas/shift/tim) is somewhere *unknown*, so open every container on the path to each match ("display all until found the specific worker"). A subject criterion therefore **wins the depth question even when combined** with a geography one — "Budi, in Rayon Pusat" must still reach Budi. `autoExpandedIds` now takes the **already-pruned** tree (documented on the function), so the geography branch opens what survived rather than re-testing ids. Two tests encoded the old rule and were rewritten; the 3 new ones were confirmed to fail against the merged implementation. Verified: web 143 suites/**2020**, tsc/eslint clean.
- 2026-07-16 — **Search now narrows the board; "Lanjutan" panel deleted.** Search was reported as unusable: searching *Taman Bungkul* left **all 9 rayons on screen at "0 petugas"**, every one collapsed. **Root cause:** the range query filters **occurrences** server-side, but the day board's tree is built from **`master`** (every rayon/kawasan/lokasi, unfiltered) — so filtering removed the *people* and left the *furniture*, and `DayBoard`'s `open` was internal `useState`, unreachable from the search. (Month/Week already coped via `subjectFiltered`; **Day was the only broken view** — the "search only works on day mode" reading was backwards.) Added two pure functions in `lib/schedules/dayBoard.ts`: **`pruneDayBoard`** and **`autoExpandedIds`**. The two criteria kinds prune **differently, by design**: **geography** (rayon/kawasan/lokasi) names a container, so the matching subtree is kept **even when empty** — "nobody is at Bungkul today" is a real answer and is exactly where you'd assign; **subject** (petugas/shift/tim) is either present or not, so empty containers are dropped. The board then **opens the ancestor chain to each match** (only nodes that hold something — a rayon filter must not blow all 87 lokasi open; the named geography opens even when empty). **Not-found is now explicit** and distinct from `emptyDay` — the two were conflated, so a bad search and an empty day looked identical — with a Hapus filter action. **`ScheduleSearch`: the Lanjutan panel is gone** (a second control surface for the same six filters, duplicating cascade state and hiding two filters behind a chevron). **Shift + Kategori Tim became autocomplete groups**, so nothing was lost; the From/To pickers only ever called `onNavigateDate`, which a date hit already does. **`CalendarFilters` deleted** — dead once the panel went (its cascade existed only to keep that panel's own selects consistent); its tests went with it, `ScheduleFilterChips`' stayed. **Fixed while there:** the autocomplete offered **rayon hits to `lockRayon` (rayon-scoped) roles** — the panel hid its rayon select but the suggestion list didn't, letting a scoped user filter to a rayon that isn't theirs. Two bugs were caught by the new tests (auto-expand not firing on **mount**, only on change; a geography filter not dropping structurally-impossible kawasan). Verified: web **143 suites / 2013** (+16), tsc/eslint clean (one *fewer* lint warning than main), i18n parity green (4 orphaned panel keys removed).
- 2026-07-15 — **Group rail on the day board (chosen over git-style tree elbows).** Each container's children now sit inside a single continuous **2px left rail in that container's own accent** (rayon → `nb-primary/40`, kawasan → `nb-info/40`) — "these belong to that" — drawn only when there are children to group. **Why not `├─`/`└─` connectors** (the ask): NB is 2px solid borders + opaque zero-blur shadows, so 1px hairlines read as a borrowed file-tree idiom; each card already carries a **6px accent left border**, so an elbow ~16px away would be a second vertical stroke doing the same job; and git's graph works because a row is *one line* — these are tall pill-bearing cards, so per-row connectors become long empty runs with a small elbow on top. One stroke per group also needs no last-child logic at any depth. (A sticky container header would address the "which kawasan am I under" long-scroll problem better than lines — noted, not built.) Verified: web 143 suites/**1997** (+3: rayon rail, kawasan rail, no rail without children).
- 2026-07-15 — **Day-board indent now steps per container (supersedes the same-day "align all lokasi" tweak).** Every container reads the same way: its own **Penempatan** block sits at the container's level (depth 0) and its children step in 16px. So `PENEMPATAN RAYON` ▸ **kawasan `ml-4`** ▸ **loose lokasi `ml-8`** (two steps: past the rayon's placement, then past the kawasan tier); inside a kawasan, `PENEMPATAN KAWASAN` ▸ **lokasi `ml-4`**. A lokasi under a kawasan therefore lands *deeper* than one hanging straight off the rayon — which is what they actually are, so depth tracks the **tree**, not the level label. (The earlier entry aligned every lokasi to one depth regardless of parent; this replaces that.) Still the hierarchy's only non-colour channel — the border hues alone fail WCAG 2.1 AA. Verified: web 143 suites/**1994**.
- 2026-07-15 — **Day-board depth now encodes the level, not DOM position.** The Rayon▸Kawasan▸Lokasi hierarchy was carried by the **left border colour alone** (primary/info/warning) — which fails anyone who can't separate cyan from amber (WCAG 2.1 AA), and read as two levels rather than three: a **loose lokasi** (no kawasan parent) is a DOM *sibling* of the kawasan cards, so it rendered at **kawasan depth**, while a lokasi *inside* a kawasan sat 16px deeper (the kawasan's 6px left border + 10px body padding). Loose lokasi now take the same **16px** nudge, so every lokasi reads one step deeper than every kawasan regardless of parentage, and indent means *level*. Verified: web 143 suites/**1993** (+2 pinning loose-indent / no-double-indent).
- 2026-07-15 — **Roll-up capacity pills on parents, assign inputs on every tier, collapsible Penempatan.** Three connected changes. (1) **Roll-up:** a parent showed nothing unless it owned the target — Rayon Barat 1 read only "11 kawasan · 87 lokasi" while each kawasan flagged `S1·0/10`, so finding the short rayon meant expanding all eleven. A parent now shows the **sum of whichever tier owns the targets below it** (`rollupTargets` + `capacityPills`), with coverage summed the same way, so the number **climbs as any kawasan/lokasi under it gets staffed**. Rolled-up pills render **dashed/muted** and carry a distinct hint — they are summed from below, not set here, and have **no gear**; an owned target stays solid. (2) **Assign anywhere:** the rayon and kawasan Penempatan tables now render on **every** tier regardless of scope (a kawasan-scoped rayon can still be scheduled at the rayon; a rayon-scoped one at its kawasan/lokasi). **Only the target stays scope-bound** — the gear and the per-role `n/target` warning appear solely on the tier `staffing_level` names. (3) Penempatan Rayon/Kawasan are **collapsible** (collapsed by default with a headcount pill) — they always render now, and an empty one shouldn't push the kawasan list off screen. Tests: 5 that assumed a parent shows nothing were updated (the same target legitimately renders twice now — owner + roll-up), +5 roll-up cases (sums 10+5→15, dashed vs solid, climbs on staffing, no pill when nothing defines one). Verified: web 143 suites/**1991** · tsc/eslint/i18n clean.
- 2026-07-15 — **Fix: opening a rayon/kawasan accordion crashed (`Cannot read properties of undefined (reading 'satgas')`).** The rayon and kawasan header pills are built from a **synthetic** per-shift group (`{ shift, countable, total }`) that has no `byRole` — an **`as BoardShiftGroup` cast** made tsc accept it, so when the per-role hint started reading `group.byRole[role]` the whole board threw. Both casts are gone (the objects now have the shape they claim), and `ShiftPill` takes an explicit `roleCounts` instead of digging into a group that, for these pills, could never have carried per-role data anyway — which also makes the hint **correct**: it now counts the subject's whole subtree, where the synthetic group would have reported 0. **Why the suite missed it:** those pills only render when the *aggregate* `capacities` map has a target, and the new tests set only `roleCapacities` — so the crashing line never ran. Added 3 tests that render the rayon/kawasan pill *with* a target; verified they fail against the reintroduced defect (2 failures) and pass once fixed. Verified: web 143 suites/**1986**.
- 2026-07-16 — **Day type resolves in WIB (was server-local), and "Hari Khusus" is retired from the picker.** `DayTypeService.resolveDayType` built its lookup date from **server-local** `new Date()` fields — but no container sets `TZ`, so the backend runs **UTC** while the business day is Asia/Jakarta (UTC+7). Between **00:00 and 07:00 WIB it resolved the previous day**: on 17 Aug at 03:00 the early shift was graded against 16 Aug's targets, and mobile's day-type badge (`current_day_type_label`, display-only) read wrong too. The web board was always right (it resolves from an ISO date string), so the two **silently disagreed** — the 2026-07-14 entry's claim of being "consistent with monitoring" held for the *logic*, not the *date*. Now uses the existing `TimezoneUtil` (`jakartaDateOf` for the override lookup, `jakartaNow(...).getUTCDay()` for the weekday) that the rest of the codebase already standardises on. **Server-TZ-independent by construction** — the suite passes under `TZ=UTC` *and* `TZ=Asia/Jakarta`; the 4 new boundary tests fail against the old impl (verified by reverting). Existing tests all used midday-UTC instants, which is why the bug survived them. Also: **`SPECIAL` ("Hari Khusus") dropped from the `HolidayManagerModal` picker.** It mapped to `DayType.HOLIDAY` identically to `HOLIDAY` (backend `mapSpecialDayType`, web `toDayType`) and has no capacity tab of its own, so choosing it was a decision that **changed nothing observable** — worse than not offering it. The enum, the API and `typeLabel` are untouched, so **pre-existing SPECIAL rows still render**; only new ones are blocked (a test pins each half). Dropped the always-dead `logger` field while in the file. Verified: be 21 day-type tests green under both TZs · web 9 holiday-modal tests green.
- 2026-07-15 — **You can now staff the tier that owns the capacity (rayon / kawasan), not just lokasi.** A chicken-and-egg made a rayon- or kawasan-level target unreachable: both assign tables rendered **only once something was already assigned there** (`rayon.placement.some((s) => s.total > 0)` / `hasPlacement`), so with an empty tier there was no `+ Tugaskan` at all and the only place to put a worker was a lokasi — a kawasan target of 10 sat at 0/10 forever. Those tables now **always render when their tier owns the capacity**. **Counting rule (operator's call): everything inside the subject counts** — a kawasan's target is met by its own placement **plus** its lokasi rosters (what the `S1·0/10` pill already did), so existing lokasi-staffed rayons don't suddenly read understaffed and assigning at the kawasan is an *option*, not a migration. Consequently the per-role coverage shown on a kawasan/rayon assign table is the **subtree's** (`subtreeRoleCounts`), not that table's own rows — i.e. its list may show 3 while the header reads 10/10, the accepted trade for not lying about the target. A lokasi passes nothing and keeps counting its own rows, since a lokasi *is* the whole subject. Verified: web 143 suites/**1983** (+3: kawasan/rayon assign tables render when empty, lokasi workers count toward a kawasan target).
- 2026-07-15 — **Capacity reads per role: the hint names it, the shift+role card warns, the editor gives each role a row.** All three complaints shared one cause — **`requirementTotalMap` summed satgas+linmas into a single number per shift**, so the role was gone before the UI saw it. That is why the hint could only ever say *"Shift 1 — 0 dari 10 **Satgas+Linmas**"* (it literally could not name the short role), why a shift+role card had no target to warn against, and why the editor mirrored the merged model with both roles crammed on one line. Added **`requirementRoleMap`** (`<subject>:<shift>:<role>` → count) alongside the aggregate (kept — the subject pill still wants a total). Now: the hint spells out the split (`Satgas 0/10 · Linmas 0/0`); each **shift+role** column shows `n/target` and flags **its own** shortfall, so the operator sees exactly which role of which shift to staff; `CapacityModal` gives **Satgas and Linmas one row each**, and the steppers' aria-labels name the role (a bare "Decrease" was ambiguous once stacked). **Targets only render on the subject that OWNS the capacity** (the tier `staffing_level` names) — a lokasi under a kawasan-scoped rayon still shows bare counts, because inventing a per-lokasi target would display a number nobody set. **korlap never gets a target** — only satgas/linmas are countable (`COUNTABLE_ROLES`). Verified: web 143 suites/**1980** (+9: role-map keying/day-type/no-summing, per-role warn/met/absent-target/korlap) · tsc/eslint/i18n clean.
- 2026-07-15 — **Capacity now honours the rayon's `staffing_level`, and is editable from master data too.** The board offered "Atur Kapasitas" on **kawasan + lokasi always, and on the rayon never** — regardless of scope. **Root cause:** `schedules/page.tsx` built the board master with `rayons.map((r) => ({ id, name }))`, **dropping `staffing_level`**; `BoardMasterData.rayons` didn't even carry it, so `DayBoard` *could not* gate and instead inferred the tier from **tree position** (loose lokasi ⇒ lokasi-level, nested lokasi ⇒ kawasan-level). That single defect produced all three reports: a kawasan-scope rayon showing the control on its lokasi, a lokasi-scope rayon showing it on its kawasan, and a rayon-scope rayon showing it nowhere. Fixed by threading `staffing_level` through (defaulting to the entity's `region` column default) and deriving **one** `capacityLevel` per rayon that gates the gear **and** the target pills. Also fixed while there: (a) the **rayon header had no gear at all** — added, plus rayon-level pills via `rayonShiftTotals()`, since `ray:` keys were already computed but never rendered (so rayon-scope capacity would have had no visible effect); (b) a lokasi **nested under a kawasan** in a lokasi-scoped rayon could never edit capacity — `RegionCard` never passed `showCapacity` to its child; (c) the synthetic **city node** is a sentinel, not a rayon, so it never offers capacity. **Backend now enforces it** (the entity comment claimed the level was "decided by the parent rayon's `staffing_level`", but nothing checked): `bulkSetForSubject` resolves subject→parent rayon and **409s** (`CAPACITY_WRONG_LEVEL`) on a wrong-tier write. **Found via the new test:** `bulkSetForLocation` **bypassed that chokepoint entirely** — it inlined a duplicate upsert loop — so the lokasi path was unguarded; it now delegates (keeping its 404-before-409 existence check) and the duplication is gone. **Non-destructive by decision:** reads stay unguarded and existing wrong-level rows are left in place (not migrated away), so they survive a rayon changing level back — they are simply never shown or counted. **New:** "Atur Kapasitas" kebab rowAction on the rayon / kawasan / lokasi grids, reusing `CapacityModal` unchanged, each gated by the parent rayon's level. Tests: two DayBoard tests **encoded the bug** (asserting a kawasan gear *and* a park gear on one rayon) and were rewritten; +6 wrong-level rejections, +3 allowed-level writes, + a reads-stay-open case. Verified: be 148 suites/**2399** · web 143/**1971** · tsc/eslint/i18n clean.
- 2026-07-15 — **Web component test coverage closed (the last Phase-4 gap).** `components/schedules/**` goes from **15 of 17 untested** to **every component covered** — 14 suites / **124 tests**. The suites pin the behaviour that silently breaks rather than the markup: the `YearView` heatmap buckets **relative to the year peak** (the ramp rescales with it; spill-over days never tint), Month/Week grids refuse to open a **spill-over day** from the neighbouring month, `WeekGrid`'s coverage **cell** (not the day header) opens the day and every rayon keeps a row so gaps stay visible, `CalendarFilters`' Rayon▸Kawasan▸Lokasi cascade **clears the narrower levels** (a stale `locationId` against a new rayon empties the board with no error), chips resolve ids to names and remove in isolation, and `ScheduleSearch` filters by a hit's own kind while a **date hit navigates instead of filtering**. `todayJakartaISODate` is mocked so the today marker asserts on the **WIB** day, not the runner's local day. Tests only — no production code touched. (PRs #264, #265, #266.)
- 2026-07-15 — **Correction + dead code removed (record was drifting).** Three claims below are **out of date** — an audit of the code proves otherwise, so treat this entry as the truth: (1) *"holiday **detection** in `dayTypeOf` still TBD"* (2026-07-14 entry) is **wrong** — holiday detection **works**: `resolveDayType(isoDate, overrides)` (web `lib/api/special-day-overrides.ts`) + `DayTypeService.resolveDayType` (backend) read `special_day_overrides` and are what the board actually uses (`schedules/page.tsx` feeds the resolved day type into `requirementTotalMap`). The `dayTypeOf()` it referred to was **dead code** (zero importers, weekday/weekend only) and is now **deleted** — `resolveDayType` is the single source of truth. (2) *"weekend/holiday capacity editing deferred"* is **wrong** — the backend accepts all three (`@IsEnum(DayType…'WEEKDAY, WEEKEND, or HOLIDAY')`, `bulkSetForSubject` upserts per `(subject, shift, role, day_type)`) and `CapacityModal` saves all three tabs. (3) The *"Still deferred"* trio on the "Jadwal polish (2)" entry — **`+ Tugaskan` scope pre-fill, edit-backfill guard, backend projection query scoping** — are all **done** (see the three entries dated 2026-07-15 below). Remaining real gap: **web component test coverage** for `components/schedules/**` (15 of 17 untested; backend scheduling specs are solid).
- 2026-07-15 — **Explicit "Ruang Lingkup" (scope) selector.** `ScheduleEventModal` replaces the single **"Seluruh Surabaya"** checkbox with an explicit **scope select** — *Seluruh Surabaya · Rayon · Kawasan · Lokasi* — that gates which geography selects appear (city → none · rayon → Rayon · region → Rayon+Kawasan · location → Rayon+Kawasan+Lokasi) and which are **required** at each level. Removes the old confusing implicit rule (scope inferred from how deep the cascade was filled); the form now carries a first-class `scope` field mapped to the API scope on save (location→static, region→mobile). When the modal is opened **pre-filled from a rayon/kawasan/lokasi board row**, the "Seluruh Surabaya" option is **omitted** (a geography-bound schedule can't be city-wide). i18n id/en (`scopeLabel/scopeHelp/scopeCity/scopeKawasan/scopeLokasi`; dropped unused `cityWide*`); tsc/eslint/i18n + schedules tests green (+2 modal tests).
- 2026-07-15 — **Projection query scoping (perf).** The range projections (`findByDateRange`, `findByDateRangeForUser`, `getDailyCounts`) + `generateRoster` no longer load **every** active `schedule_event` into memory — the event `find` is now DB-scoped to events that can occur in `[from, to]` (`start_date ≤ to AND (end_date ≥ from OR end_date IS NULL)`, via a shared `activeEventsOverlapping` helper). Correctness-preserving: an event outside the window expands to zero occurrences anyway. Live-verified: year-summary returns 637 occ for Jul–Aug 2026 + projects open-ended dailies to 2030, but **0** for 2025-Q1 (before any event starts); 87 schedules unit tests green.
- 2026-07-15 — **Edit-backfill guard.** When editing an event whose location/kawasan isn't in the loaded master data (deleted/renamed), the create/edit modal now **toasts** "…re-pick the placement" and marks the backfill done, instead of silently showing only the rayon and looking half-broken. Distinguishes still-loading (retry) from genuinely-missing (warn).
- 2026-07-15 — **`+ Tugaskan` pre-fills the placement.** Clicking "+ Tugaskan" on a board row now opens the create modal **pre-filled at that subject** — no re-picking the Rayon▸Kawasan▸Lokasi cascade. The day board threads each container's geography (rayon / kawasan / lokasi, or **city** for the Seluruh-Surabaya node) + the clicked shift into a context-bound `onAssign` (`AssignContext`), which the page passes to `ScheduleEventModal` as `initialRayonId/RegionId/LocationId/ShiftId/CityWide`. Assign at kawasan level (mobile) from the kawasan placement, or at a specific location (static) from a location card. tsc/eslint/i18n + dayBoard/schedules tests green.
- 2026-07-15 — **Jadwal polish (2).** Hari-Libur panel gains an **in-panel year switcher** (‹ year › — no close/reopen for multi-year entry; resets to the calendar year on open). Capacity modal shows a **loading state** while fetching a subject's requirements (was blank/zeroed steppers). Day board shows a **"Belum ada jadwal"** banner when the day has no occurrences (was an ambiguous empty tree). i18n id/en; tsc/eslint/i18n + tests green. *(Still deferred: `+ Tugaskan` scope pre-fill (needs container context threaded through ShiftRoleTable), edit-backfill guard, backend projection query scoping.)*
- 2026-07-15 — **Jadwal polish + overlap-guard hardening.** Day board now orders **locations and workers alphabetically** (was occurrence order); `DateNav`'s prev/next **aria-labels are localized** (id/en keys added — were English fallbacks). Documented the `ScheduleOverlapService.shiftWindow` WIB-as-UTC invariant so it isn't mis-"fixed" (comparison-only; ordering + midnight-crossing preserved). Behaviour-preserving; tsc/eslint/i18n + overlap/dayBoard tests green. *(Deferred polish: `+ Tugaskan` scope pre-fill, edit-backfill guard, holiday-manager year selector, capacity-modal loading/discard, projection query scoping — a later pass.)*
- 2026-07-14 — **City ("Seluruh Surabaya") schedule scope.** `ScheduleScope` gains **`city`** — a whole-Surabaya placement with no rayon/kawasan/lokasi, for a Tim Patroli (individual or team). Parallel to the rayon-scope addition: migration `17498000000000` widens `chk_schedule_events_scope` to accept city (all geo FKs null); `validateEventShape` city branch (rejects any geo id; requires a city-scope role); the materializer + both projections already fall through to a null subject. **Visibility:** city events cover every rayon, so they're visible to rayon-scoped users (added to the list/findOne scoping). Web: `ScheduleEventModal` gains a **"Seluruh Surabaya (Tim Patroli)"** toggle that hides the rayon/kawasan/lokasi cascade; the day board renders a top **"Seluruh Rayon / Surabaya"** node (`CITY_NODE_ID`, placement-only) above the rayons. Tests: +3 backend (city validation/accept) + a dayBoard city-node test; tsc/eslint/i18n green.
- 2026-07-14 — **Correction:** an earlier line here (and commit `c3434c9d`) claimed a mobile
  **Hari/Minggu/Bulan** switcher; that was **never coded**. The worker `MyScheduleScreen` is **day-only** —
  a single-day roster with an `NBDatePicker` + prev/next/today navigation, fed by `getMyRange(date,date)`
  → `GET /schedules/range` (backend self-scopes workers; includes projected rows). Day-only is a deliberate
  simplicity choice for field workers. A week/month switcher + a read-only **supervisor** viewer
  ("Jadwal Petugas") are **Phase-6 mobile** work — see [`../../REVAMP-STATUS.md`](../../REVAMP-STATUS.md).
- 2026-07-14 — **Rayon-wide schedule scope (P5, ADR-047 amendment).** `ScheduleScope` gains **`rayon`** — an event placeable at whole-Rayon level (no fixed location/region), e.g. a roving crew. New `schedule_events.rayon_id` (FK → rayons, indexed) + the `chk_schedule_events_scope` CHECK widened to `(static→location) | (mobile→region) | (rayon→rayon)` (migration `17495000000000`). Wired end-to-end: `validateEventShape` rayon branch (rayon_id required, location/region forbidden, rayon-scope enforced), create/update/split paths, rayon-scoping list + `findOne` checks, the materializer + **both** projection sites (`findByDateRange` + `getDailyCounts`) derive rayon from `event.rayon_id`. Web: `ScheduleEventModal` gains a **Rayon** scope option + rayon picker; `buildDayBoard` adds a rayon-level "Penempatan Rayon" placement bucket; week/month summaries map rayon-scoped occurrences via `rayon_id`. Backend unit tests (rayon validation + accept) + web dayBoard tests; live-verified end-to-end (create → materialize → range → negative 400). Only satgas/linmas still count toward understaffing.
- 2026-07-14 — **Holiday / special-day management surfaced on the Jadwal page.** The backend `special_day_overrides` (date · WEEKEND/HOLIDAY/SPECIAL · name; already feeding monitoring's `DayTypeService` + seeded with Indonesian national holidays) had no web UI. Added a **Hari Libur** button on the Jadwal toolbar → `HolidayManagerModal` (list per year + add/remove, `schedule:create`-gated; backend `USER_MANAGERS` + unique-date 409 enforce). Web `special-day-overrides` API hooks + `resolveDayType(iso, overrides)` now drives the day board's capacity lookup, so **HOLIDAY/WEEKEND staffing targets fire on marked days — consistent with monitoring** (was weekday/weekend-only on the board). No backend change.
- 2026-07-14 — **Year-load heatmap + weekend/holiday capacity (redesign follow-ups).** `CapacityModal` gains Weekday/Weekend/Holiday tabs (backend already upserts per `(shift, role, day_type)`; holiday *detection* in `dayTypeOf` still TBD). Year view mini-calendars now colour each day by load via a new **`GET /schedules/year-summary`** (RANGE_VIEWERS, 366-day cap, workers self-scoped / rayon-scoped roles pinned) backed by `SchedulesService.getDailyCounts` — a lightweight aggregate that counts materialized + projected occurrences per day, deduped against a single tombstone query (no per-event round-trip, no row hydration); normalizes `getRawMany` Date columns + NONE-recurrence Date dates to `YYYY-MM-DD`. Live-verified end-to-end.
- 2026-07-14 — **Jadwal UX redesign (P1–P4 + P6 start).** Web Schedules page rebuilt around a single range select (Tahun/Bulan/Minggu/Hari, default **Hari**) with drill-down; the table view is retired in favour of search + filter chips. **Day = coverage board** (Rayon ▸ Kawasan ▸ Lokasi tree, one branch open at a time, each level rendering the shared shift + role-column layout; scales to thousands of workers by rendering nothing until a branch opens). Level identity via light neutral headers + a per-tier accent left border (rayon=sage, kawasan=cyan, lokasi=amber; dark-mode safe). **Year** = 12 mini month calendars (day → day view, month name → month view). **Month** = per-day, per-rayon breakdown. **Week** = every rayon (incl. empty) × day, each cell summarized by shift **and** role (Satgas/Linmas/Lainnya/Tim spelled out; compact `S{n}` pills carry full-name tooltips). Hybrid **search** (grouped autocomplete + Lanjutan panel reusing `CalendarFilters` + two From/To date pickers). Click → read-only detail → Ubah/Hapus with this / this-and-future / series scope-on-save (two-editor model: series edits via `ScheduleEventModal`, single-day via the roster row editor). **Capacity converged on the existing `location_staff_requirements`** (per location×shift×role satgas/linmas×day_type) which already feeds monitoring — understaffing pills compare scheduled satgas+linmas vs the day-type's summed requirement; `CapacityModal` edits WEEKDAY (gear per location, admin_system/superadmin). Mobile `MyScheduleScreen` gained day navigation (personal day calendar). **Deferred:** P5 rayon-scope schedule model, weekend/holiday capacity editing, year-load heatmap (needs an aggregate endpoint), full mobile week/month parity. Grouping logic in `apps/web/src/lib/schedules/dayBoard.ts` (unit-tested). Every commit verified green (web tsc/eslint/i18n + jest); staging untouched.
- 2026-07-13 — **"Team type" renamed to "team category" (Kategori Tim).** `schedule_events`/`schedules` columns `team_type_id` → `team_category_id`, relation `team_category`, catalog table `team_categories` (rename migration `17494000000000`). See teams spec.
- 2026-07-13 — **Phase 4 adjustments (user decisions).** (1) **Teams are type-only**: the `/teams` page manages the crew-type catalog (`team_categories` + `marker_image_url` + bubble `marker_color`); the concrete team (name = event `title`, PIC, members, when/where) is defined per schedule event — `teams` table dropped, `schedule_events`/`schedules` re-pointed to `team_category_id` (migration `17492800000000`, which also drops the LEGACY `UQ_daily_schedules_user_date` index that still blocked multi-shift days on migrations-built DBs). (2) **Overlaps warn, don't block** (Google-Calendar-style): overlapping occurrences are created and reported per member/date in `materialization.conflicts[]`; only exact duplicates (same worker+day+shift) are skipped (`duplicate`). (3) **Calendar projection**: `GET /schedules/range` merges virtual `is_projected` rows expanded from active events beyond the materialized horizon (dimmed dashed chips; edit choosers hide "hanya hari ini" for projected days) — a permanent event is now visible arbitrarily far ahead; the horizon setting only bounds what is pre-generated. (4) **Workers see their own calendar**: `/schedules/range` allows satgas/linmas/korlap, server-side-scoped to their own rows. Live-verified on a fresh scratch DB (61-migration chain + seed + booted API: type CRUD w/ colour, team event via type, overlap created-with-warning, +45-day projection, satgas self-scope).
- 2026-07-12 — **Phase 4 review pass (4-agent audit → fixes).** Engine: `rematerializeSeries` deleted only the exact from-date (stale future occurrences after a series edit) → `>= from`; overlap candidate query bounded to the 3-day window; batched tombstone lookup (was a query per member×date). Multi-shift correctness (the old one-row-per-day legacy): `POST /schedules` (add-for-day) now rejects only true time-window overlaps — a second non-overlapping shift is legal; `findByUserAndDate` is now deterministic + **time-aware** (with multiple shifts it returns the shift covering "now" WIB, else the next upcoming — this feeds `GET /schedules/my`, clock-in shift/areas resolution, and monitoring's rayon stamp); `findAllByUserAndDate` added for full-day consumers; `replaceWorker` checks every row of the stand-in's day; monitoring roster summary counts **distinct workers**, not rows. Events API: `DELETE ?scope=this` was a silent 204 no-op → now soft-deletes that date's occurrences (tombstones); past-date guards on this/this_and_future edit+delete (400); series/split updates run the same shape validation as create (scope↔location/region coherency, rayon scope, recurrence-config rules incl. `specific_dates ⊆ [start,end]`, weekly needs weekdays) so bad edits 400 instead of DB-CHECK 500s; split copies members when `member_ids` omitted; `validateUserRoles` rejects unknown ids + inactive users; `member_ids`/`dates` array caps (100/120); list from/to filters fixed (from-only returned everything). Web: WIB "today" anchoring (non-WIB browsers drifted a day), chip clicks no longer bubble into day-cell create, conflict toast shows "+N more", event-load failures surface a toast, `source` type gains `'event'`.
- 2026-07-12 — **Phase 4 landed (ADR-047).** `schedule_events` + members + recurrence expansion util; `ScheduleMaterializerService` (rolling horizon, tombstones, detached overrides); time-based `ScheduleOverlapService` (roster unique now `(user,date,shift)` — migration `17492600000000`); `/schedule-events` CRUD (`@RequirePermissions schedule:*`, rayon-scoped, per-member conflict reporting) + `GET /schedules/range` (62-day cap); **template generator retired** (migration `17492700000000` converts standing templates → daily events; users without a location stop being auto-rostered until scheduled); demo seed seeds events + today's roster. Web: calendar-first Jadwal page (Bulan/Minggu/Hari + Tabel), `ScheduleEventModal` (recurrence incl. every-N/weekly/specific-dates, team PIC+members, static/mobile), edit/delete scope choosers (this / this-and-future / series), conflict warnings. Verified live on a scratch DB end-to-end (recurrence materialization, duplicate→overlap conflicts, team fan-out with `team_id` rows, series split, series delete, scope 400s, rayon scoping). Fixed en route: nested `recurrence_config` DTO whitelisting (`@Type(() => Object)` rejected all keys), version-pinned `@IsUUID('4')` vs deterministic v5 seed ids.
- 2026-07-12 — **Area→Location terminology sweep.** Renamed multi-location, schedule_locations → schedule_locations, location_id field.
- 2026-07-10 — Revamp target set: rule-based recurrence + occurrences, calendar UI, teams, static/mobile (ADR-047/048).
- 2026-07-10 — Spec created in product-docs restructure. History: [`../../history/CHANGELOG.md`](../../history/CHANGELOG.md).
