# Monitoring

**Status:** ✅ Active · **Backend:** `monitoring`, `location`, `gateways` · **Key ADRs:** ADR-011→029 (event-sourced status), ADR-016 (Redis scaling), ADR-006 (location partitioning)

## Overview
Real-time supervisor dashboard: live worker positions, five-status tracking, and an aggregate-first drill-down (Surabaya → rayon → location → workers) over Google Maps, with Socket.IO push. Scope is authorized by role (city / rayon / location). Backed by the `monitoring` module (services + cron under `apps/be/src/modules/monitoring`) and the `location` module (ingest + history). **Under revamp** post-UAT (a top UAT-feedback area).

## Key decisions
- **Event-sourced status via Redis Streams** (ADR-029, supersedes ADR-011) — status is derived from a location/event stream, not a materialized column; `monitoring/cron` recomputes aggregates.
- **Redis for WebSocket scaling** (ADR-016) — Socket.IO adapter + notification retry.
- **Location log partitioning** (ADR-006) — monthly partitions for query performance.
- **Aggregate-first drill-down** — bubbles show active-inside-location / scheduled ratios; markers standardized web + mobile.
- ⚠️ The legacy `supervisor` module is **deprecated** — superseded by this feature; do not extend it.

## Revamp notes (post-UAT) — target model (ADR-046)
> **Status: deferred (Phase 5).** Rollout tracking + the remaining checklist live in
> [`../../REVAMP-STATUS.md`](../../REVAMP-STATUS.md). Understaffing on the map must use the polymorphic
> staffing subject (region/location/rayon/city) from the Phase-4 consolidation, not location-only.
- **Subject model** — *monitorable* (anyone clocked-in: satgas, linmas, korlap, kepala_rayon, admin_rayon) vs *scheduled/staffing-counted* (**only satgas + linmas**). Non-scheduled clock-ins surface only via daftar petugas / search.
- **Presence & attendance model** ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md), supersedes 046's status enum) — three **derived** axes, no stored lifecycle status: **attendance lifecycle** (`tidak_bertugas` · `belum_hadir` · `terlambat` · `bertugas` · `pulang` · `tidak_hadir`, one/worker/service-day — only `bertugas` renders live), **live presence** (`aktif`/`offline` + `dalam_area`/`luar_area`/unknown + one position field: current when aktif, last-known when offline), and **counting** (staffing = `bertugas` ∧ scheduled-for-subject ∧ satgas/linmas; ad-hoc & monitorable roles show but don't count). Policy locked: per-shift grace (late 15 min / no-show at end); forgotten clock-out **never auto-closed** (`lupa_clock_out` flag, corrected by supervisor; lembur is explicit only); excused reasons cuti/sakit/izin/libur set by admin_rayon/korlap; offline threshold 10 min configurable, korlap paged at threshold within shift window only. All resolved against the shift's **service day** (cross-midnight safe for shift 3).
- **Supervisor visibility** (hierarchical, server-enforced) — management → all Surabaya; kepala_rayon/admin_rayon → their rayon; korlap → their region (+ optional location) incl. team members; satgas/linmas → none.
- **Static vs mobile** — static geofenced to their location; mobile (e.g. penyiraman) geofenced to their region, same tolerance as locations ([ADR-045](../../architecture/decisions/ADR-045-four-level-location-hierarchy.md)). Drill visibility: rayon = all; region = mobile-of-region + static-in-its-locations; location = static-of-location.
- **Drill** — **remove the Surabaya bubble**; draw all rayon boundaries on first load with per-rayon bubbles; no workers at top level. Drill: Rayon → Region → Location → workers.
- **Bubble** = marker + active-count with status-tinted ring; **hover** = `total / active / by_shift{s1,s2,s3} / by_role / understaffed` at that scope. **Teams** render as one group bubble (member count + team name) that expands to members ([teams](../teams/README.md)).
- **Understaffing** counts only satgas+linmas vs `LocationStaffRequirement`.
- **Search** — server-backed, scope-filtered; matches worker name / location / team keyword; returns anyone clocked-in with a location fix in the last 24h (incl. non-scheduled). Status rings follow the presence model above ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md)).
- **Perf** — keep event-sourced Redis streams (ADR-029) + in-place snapshot patching (no map remounts); server-computed bubble aggregates; rooms `monitoring:rayon|region|area:{id}`.
- **Web first**, then mobile parity after client/PM design ack.

## Implementation
- **API:** [`../../api/contracts.md`](../../api/contracts.md) · errors [`../../api/error-handling.md`](../../api/error-handling.md) (live Swagger `/api/v1/docs`)
- **Database:** [`../../database/schema.md`](../../database/schema.md)
- **Web:** Monitoring page (map + drill-down + roster) — [`../../platforms/web/pages.md`](../../platforms/web/pages.md)
- **Mobile:** Map Dashboard — [`../../platforms/mobile/screens.md`](../../platforms/mobile/screens.md)

## Related features
- [geography](../geography/README.md)
- [attendance](../attendance/README.md)
- [notifications](../notifications/README.md)
- [scheduling](../scheduling/README.md)
- [teams](../teams/README.md)

## Changelog
- 2026-07-17 — **Worker pins honor each role's configured marker icon.** Previously `workerPinIcon` drew a hardcoded glyph per role code (satgas=hard-hat, linmas=shield, korlap=clipboard, everything else = a generic person) and ignored the data-driven role's `marker_icon` (ADR-044) — so a custom role, or an overridden icon, never showed on the map. Now the snapshot/live-users worker carries **`role_marker_icon`** (resolved from the `roles` table via a per-request `buildRoleMarkerMap`), and the web resolves the pin glyph in priority order: the role's explicit `marker_icon` → the built-in bespoke glyph for satgas/linmas/korlap → the role's seeded default icon (`ROLE_MARKER_ICONS`: crown/building/database/…) → a generic person. A new `ICON_GLYPHS` registry covers the seeded marker names (user, hard-hat, clipboard, briefcase, shield, star, database, crown, building, settings, key, droplets). **Color stays live-status-driven** (green aktif / amber offline) — only the glyph is role-configurable. **Seed fix:** satgas and linmas were both seeded `marker_icon: 'shield'` (indistinguishable pins) — satgas is now `hard-hat` (`role-seeds.ts`), matching its prior built-in look and distinct from linmas. be 368 + web 279 tests pass (new: role-glyph resolution + Role-repo provider in 2 specs). Verified live: `GET /monitoring/live-users` returns `role_marker_icon: "hard-hat"` for a satgas worker.
- 2026-07-17 — **Declutter the base map + Google-style place labels + custom marker images (raster path).** Three map polish items, all in-code on the existing raster map (no Map ID / Advanced Markers — deliberately, since the in-code `styles` POI hiding only works without a Map ID). **(a)** `MAP_OPTIONS.styles` (`DECLUTTER_STYLES`) hides third-party POIs — businesses, restaurants, medical, worship, schools, attractions, transit — keeping park geometry + road labels, so only our own area markers stand out. **(b)** Each node marker shows its **name below the pin** (`node-marker-label`: dark text + white halo, `labelOrigin` under the icon), like a Google place label. Dense kawasan/lokasi labels reveal only at `zoom ≥ AREA_LABEL_REVEAL_ZOOM` (14) so they don't collide at the drill-fit zoom; rayons (few, spread out) are always labeled; empty muted-dot lokasi are never labeled. **(c)** A **custom-uploaded `marker_image_url`** now renders **literally** as the pin (`nodeImageIcon`); its count/status lives in the status bar since legacy markers can't composite a badge+ring onto an external image (that would need the Advanced/DOM markers we opted out of for the POI-hiding tradeoff). web 312 tests pass (4 new: rayon-always-labeled, zoom-gated kawasan label, no-label muted dot, custom image icon).
- 2026-07-17 — **Node markers honor the "bawaan sistem" default per kind (no more bare dots).** The rayon/kawasan/lokasi editors expose a *Penanda Peta* that falls back to a system-default pin (`entityMarkerDefault`: rayon → orange building, kawasan/lokasi → green tree), but the map ignored it — rayons rendered a plain gray circle. `NodeMarkerLayer` now derives a glyph per node: its explicit `marker_icon` if set, else `KIND_DEFAULT_GLYPH[kind]` (rayon → `building`, kawasan/lokasi → `trees`), so **every rayon/kawasan shows a kind-appropriate marker** with the active-count badge + health ring. Empty lokasi (nothing scheduled, no explicit icon) still collapse to a muted dot to fight clutter. **Follow-up:** rendering a *custom-uploaded* `marker_image_url` (arbitrary PNG/SVG) composited with the count+ring still needs the AdvancedMarker/DOM migration — the field is plumbed through the aggregate node + `NodeMarker` but the interim uses the named glyph. web 308 tests pass.
- 2026-07-17 — **Mobile-friendly node markers: drop hover-stats + render each area's configured marker glyph.** Two fixes for touch use. **(a) No more hover for stats** — `NodeMarkerLayer` dropped the hover InfoWindow (touch has no hover); **tap still = drill**, and a node's stats are read from the re-scoping status bar + the tappable current-node marker (the native `title` keeps a name hint on desktop). **(b) Markers use the area's configured `marker_icon`** — the aggregate node now carries `marker_icon` + `marker_image_url` (from the rayon/kawasan/lokasi entity), and `nodeCountIcon` renders the named glyph (e.g. kawasan `"trees"`) with the active count on a health-colored badge + the status ring; falls back to the plain count dot when none is set. **Note:** pre-seeded **rayons have no marker set** (only *kawasan* get `marker_icon: "trees"` via the region seed), so rayon pins stay the count dot until a marker is configured; **`marker_image_url` (external PNG/SVG) is plumbed through but not yet composited on the pin** — that needs the AdvancedMarker/DOM migration (follow-up). Verified live: drilling Rayon Timur 2 shows its kawasan as tree-glyph pins with count badge + ring. be 368 + web 307 tests pass (2 new glyph tests).
- 2026-07-17 — **Fix 3 verification findings: ad-hoc pill, filter/bar mismatch, team search.** From the systematic Playwright verification. **(1) Ad-hoc workers were invisible in the city/rayon pills** — "Luar jadwal" was hard-0 above area scope, so an active clocked-in-but-unscheduled worker showed in neither Aktif (scheduled-only) nor Luar jadwal. `getAggregate` now computes `off_schedule_count` (clocked-in ∖ current-shift roster) at city/rayon/region scope; the web reads it into the Luar jadwal pill. **(2) Filter panel disagreed with the status bar for the same labels** — the filter's chips used raw tracking-status `totals` (Aktif 6 incl ad-hoc, Tidak Hadir 0) while the bar used the presence/roster model (Aktif 5, Tidak hadir 3). `statusCounts` now derives from the same `presence_totals`/`roster_totals` above area scope, so the two always agree. **(3) Team-keyword search returned nothing** (deferred in 5.7a) — `GET /monitoring/search` now also matches a **team name** via an `EXISTS` subquery on today's roster (`team_categories.name ILIKE`, keyed by the Jakarta service date, no row duplication). Verified live: bar+filter both read `Aktif 5 / Offline 1 / Tidak hadir 3 / Luar jadwal 1`; searching "Penanaman" returns its 3 members. be 368 + web 305 monitoring tests pass (aggregate spec stubs `scheduledUserIdsForCurrentShift`; search spec asserts the team EXISTS clause).
- 2026-07-17 — **Area boundaries carry their configured colors + kawasan outlines drawn at rayon zoom** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md); "use the marker set in the area's data"). Boundaries now use each area's own styling: rayons already tinted; **kawasan (region) boundaries** are now returned by `GET /monitoring/boundaries` (new `RegionBoundaryDto` + `regions[]` on each `RayonBoundaryDto`, with `border_color → fill_color`, polygon + centre) and **drawn tinted at rayon/region scope** (`showRegionPolys`); **lokasi boundaries** gained a `color` field (border→fill) applied to their outline/fill where they render. The marker itself is unchanged — status ring + active-only count — so identity lives on the boundary, status on the marker (per the agreed split). Backend: `getBoundaries` fetches the rayon's regions + maps colors (all three return paths carry `regions`). web tsc + be tsc clean; 305 monitoring/map/lib tests pass (RayonBoundary fixtures gained `regions: []`). Verified live via Playwright: drilling Rayon Timur 2 draws the 17 kawasan outlines in their `#1C1917` color inside the rayon polygon. Data: lokasi have no colors yet (all null → default tint); kawasan have colors + `marker_icon: "trees"` (icon not yet rendered on the pin — available follow-up).
- 2026-07-17 — **Stats pills align with the selected scope at kawasan level.** At region (kawasan) scope the top status pills (Aktif/Offline/Tidak hadir/Luar jadwal) read `activeAgg` — which is the parent **rayon** aggregate — so they showed the whole rayon's numbers while the roster panel correctly listed only the kawasan's lokasi. New `regionTotals` memo sums the selected kawasan's lokasi nodes (`counts_by_status` + `presence` + `roster`) so `statusCounts`/`presenceCounts` match the selected kawasan. Rayon/city/area scopes unchanged (already aligned). web tsc + 27 page/side-panel tests pass.
- 2026-07-17 — **Workers reveal by zoom, not only by clicking into an area** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md) "Drill & bubbles": *workers render only at deeper zoom*). `SimpleMonitoringMap` now computes `renderWorkers = showWorkers || (zoom >= WORKER_REVEAL_ZOOM(15) && (scope==='rayon' || scope==='region'))`, so once you zoom past the area threshold inside a rayon/kawasan the individual worker pins take over from the node markers — no drill-to-area click needed (the snapshot workers are already loaded at rayon/region scope). City/Surabaya scope never reveals workers by zoom (too many). Verified live via Playwright: at Rayon Timur 2 scope, focusing a worker zoomed to street level and rendered the "Satgas Timur 1 Dua" pin (purple outside-area ring) while the breadcrumb stayed "Rayon Timur 2". 2 new `SimpleMonitoringMap` tests (rayon+deep-zoom → worker pins; city+deep-zoom → node markers stay); 305 monitoring/map/lib tests pass. **Remaining ADR-046 follow-up:** kawasan boundaries at rayon scope (lokasi boundaries already draw).
- 2026-07-17 — **Node markers replace ratio bubbles + zoom-gated kawasan/lokasi tiers** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md) "Drill & bubbles": *replace the disliked active/terjadwal ratio bubbles with a marker + active count + status ring; workers render only at deeper zoom*). New `nodeCountIcon(variant, active, health)` — a compact white dot with a health-tinted ring (green when `clocked_in ≥ scheduled`, red when nobody's in, amber partial, gray empty) and the **active worker count**; empty lokasi collapse to a small muted dot so dense rayons stay legible. `NodeMarkerLayer` swaps `nodeRatioIcon` → `nodeCountIcon` at every node scope; `NodeMarker` gains `active`. **Zoom-gated tiers** (decided with the user after a live density test: showing all ~180 lokasi at rayon zoom rendered 242 markers — heavy + cluttered): at **rayon** scope the map now shows **kawasan markers + only the lokasi that belong to no kawasan** (region-less); a kawasan's own lokasi appear when you drill into it; **region** scope shows that kawasan's lokasi; workers still render at **area** scope. Verified live via Playwright — Rayon Pusat dropped 242 → ~24 node markers (clean). **Data note:** 363/953 lokasi (38%) have no `region_id` (kawasan assignment incomplete in seeds — e.g. Rayon Timur 2 has 65 region-less lokasi, so it stays busy until they're assigned; Rayon Taman Aktif is region-less by design). **Still TODO for the full ADR-046 interaction:** worker reveal should be *zoom*-driven (currently click-to-area-scope), and kawasan boundaries aren't drawn at rayon scope yet (lokasi boundaries are). web tsc + lint clean, 19 monitoring/map/lib suites / 303 pass (`NodeMarkerLayer.test` rewritten for the count marker + muted-empty-dot).
- 2026-07-17 — **Fix the drill-bubble ratio (Playwright verification finding).** The rayon/kawasan/lokasi map bubbles rendered `active_inside / scheduled` instead of the intended attendance ratio `clocked_in / scheduled` (hadir/terjadwal) — `NodeMarkerLayer` passed `node.active_inside` where `nodeRatioIcon(variant, scheduled, clockedIn)` expects `clockedIn` (its doc/params/tooltip + the `rosterHealth` color all assume clocked-in). Effect: a **fully-present park read understaffed** the moment a worker went offline (GPS dropped) or stepped outside their polygon, since both drop out of `aktif.dalam` — and the bubble disagreed with the side-panel's `Hadir`. Now the numerator is `clocked_in` (present = active **or** offline, all-roles roster — same set as the denominator and the panel), so the ratio + health color are consistent across the map, the bubble, and the panel. Verified live via Playwright: Timur 2 `0/2 → 2/2` green, Taman Aktif `1/2 → 2/2`, Barat 1 `0/3 → 1/3` (genuinely understaffed). New `NodeMarkerLayer.test.tsx` (3) guards the wiring; `active_inside` stays a hover-tooltip detail. web tsc + 14 monitoring suites / 265 pass. *Not fixed (documented, lower impact): the roster denominator still includes non-staffing roles like korlap — consistent with the panel, and `required` handles the satgas+linmas target separately; the snapshot's stored-status read lags a stale worker by the sweep interval (both snapshot + aggregate read the same stored column, so the two views stay consistent).*
- 2026-07-16 — **Phase 5 high-review fixes** (the capstone HIGH-effort review across `7f2ef586..HEAD`, 3 parallel reviewers). Two real findings fixed in `buildAreaSummaries` (the snapshot side-panel staffing path); the reviewers' other higher-severity flags were verified **false** (korlap can't call `/aggregate` — not in `MONITORING_RAYON` — so no scope leak; area aggregate nodes DO carry `region_id`, so the web region back-nav is intact). **(1) HIGH — counted `ACTIVE` only.** The side-panel `active_count` ignored OFFLINE workers, so a fully-present park read understaffed the moment signals went stale — contradicting the model + the aggregate. Now counts **active + offline (clocked in)**. **(2) N+1** — the requirement lookup ran one query per area (a 100-area city snapshot fired 100 queries); now a single batched `location_id IN (...)` query. Existing snapshot contract test updated (active_count 1 → 2, reflecting the offline worker now counted). Known deferral unchanged: `buildAreaSummaries` still counts ad-hoc (the aggregate excludes them via `countScheduledOnline`). be tsc + monitoring 18 suites / 368 pass.
- 2026-07-16 — **Phase 5.6c — drop the top-level Surabaya bubble** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md): "remove the Surabaya bubble; draw all rayon boundaries on first load"). The map now **opens directly on the rayons** — `'surabaya'` is removed from the `Scope` union; a city-scope role lands on `{ scope: 'city' }` showing rayon bubbles + boundaries, no single summary node. Removed the `SurabayaSummaryCard` (deleted the now-orphaned component), the `surabaya` node-marker + drill branch, and the `city→surabaya` drill-up (city is the top; no drill above the rayons). tsc-guided clean removal — every dead `'surabaya'` branch + unused symbol (`drillToCity`, `cityRosterTotals`, `EMPTY_ROSTER`, …) removed. ⚠️ **Browser-pending:** the first-load visual (rayons drawn immediately) verified via tsc/unit only. Full web suite green (147 suites / 2088).
- 2026-07-16 — **Phase 5.7d — team bubbles on the web map** ([ADR-048](../../architecture/decisions/ADR-048-teams.md)). Workers on the same team collapse into one **team-colored bubble** (member count + team name) that reveals individual member pins when you zoom past level 17 — reusing the existing supercluster zoom-to-expand path (a team click zooms in; no new page state). New **pure** `lib/monitoring/teamGrouping.ts` `groupWorkersByTeam(workers, zoom, expandZoom=17)`: below the threshold it collapses each ≥2-member team into a `TeamGroup` at the members' **centroid** (name/color from the group, all-non-finite-coords teams fall back to individuals); at/above it returns everyone individual. `teamBubbleIcon` renders a 90×46 team-bordered bubble (gray fallback when `team_color` is null). `SimpleWorker` + the page's `mapWorkers` memo now carry the team fields; `WorkerClusterLayer` feeds workers + team groups to supercluster with a `kind` discriminant. 13 pure grouping tests. **Known limitation (browser-pass):** a low-zoom *proximity* cluster containing a team counts it as one point, so its count bubble undercounts headcount there. ⚠️ **Browser-pending:** the bubble draw + expand interaction were verified via tsc/unit only.
- 2026-07-16 — **Phase 5.7c — team membership on the snapshot** ([ADR-048](../../architecture/decisions/ADR-048-teams.md); the backend for team bubbles). Each live worker now carries `team_id` / `team_name` / `team_color` on `LiveUserDto` + `SnapshotWorker` (+ optional on the web/mobile types), so the map can collapse team members into one group bubble — the team is a **rendering** grouping only, no geofence/counting change. New `SchedulesService.getTeamMembership(userIds, date)` does a **single batched** query (no N+1) left-joining `team_category`, filtered to `PLANNED`/`PRESENT` team schedules, and keys each worker by the ADR-048 grouping id `schedule_event_id ?? team_category_id` (name + `marker_color` from the category; first team schedule wins per worker). `getLiveUsers` calls it inside its existing `Promise.all` and guards the optional roster service. Medium review tightened the query to `PLANNED`/`PRESENT` only (a worker off/replaced for a team schedule isn't on that bubble). **Browser-pending:** the group-marker rendering + expand-to-members that consumes these fields is the remaining visual piece of 5.7.
- 2026-07-16 — **Phase 5.7b — web search box wired to the server.** `MonitoringSearch` now queries `GET /monitoring/search` (new `useMonitoringSearchQuery`, debounced 250 ms, enabled only when the dropdown is open + term ≥ 2 chars) for the **petugas** section, merged with the client snapshot results **deduped by id (server wins)** — so clocked-in workers not in the loaded snapshot (ad-hoc, off-screen, another drill scope) now surface. Area + rayon search stay client-side over loaded boundaries. Client results still show while the server query is in flight (no empty flash). **Medium review caught 5 broken tests** the delegate missed — adding `useQuery` to the component made `MonitoringSearch.test` + `page.test` throw "No QueryClient" (they render it without a provider); fixed by stubbing `useMonitoringSearchQuery` in both. Full web suite green (146 suites / 2075). Team-keyword results arrive with the team bubbles (5.7).
- 2026-07-16 — **Phase 5.7a — server-backed monitoring search** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md)). New `GET /monitoring/search?q=` (guard `@Roles(...MONITORING_AREA)`, scope-filtered via `applyScopeFilters`) returns workers **clocked in with a fix in the last 24h** whose name or lokasi matches — reusing `getLiveUsers` by adding an optional `q` to its filter (an escaped `ILIKE` on `user.full_name` / `area.name` + a `last_location_at >= NOW() - 24h` bound, applied only when `q` is set). Because the query keys off `shift_id IS NOT NULL` rather than the roster, **monitorable-but-unscheduled clock-ins surface too** (the ADR's way to find people working without a schedule). Empty term short-circuits to an empty result with no DB hit. LIKE metacharacters in the term are escaped (no wildcard injection). **Deferred:** team-keyword matching (needs a schedules→team_categories join; lands with the team bubbles in 5.7). Tests: controller (blank term → empty, no query; real term → scoped `getLiveUsers`) + service (the ILIKE + 24h filter is applied). Replaces the client-side `useMonitoringSearch` snapshot filter; wiring the web search box to this endpoint is a small frontend follow-up.
- 2026-07-16 — **Phase 5.6a — web region (kawasan) drill tier** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md)). The web map drill becomes Rayon → **Kawasan** → Lokasi → workers, with a **region-less fallback**: at rayon scope the page fetches the region aggregate and, if it returns ≥1 kawasan, shows those bubbles; if the rayon has no kawasan (e.g. Taman Aktif) it falls straight through to lokasi bubbles as before. At region scope the lokasi are the rayon's area nodes **filtered by `region_id`** (no extra fetch — 5.5a put `region_id` on area nodes). New `drillToRegion` + region cases in the marker/list drill dispatch + `goBack` (area→kawasan→rayon), region marker variant (reuses the rayon/area bubble), `AggregateNode.type` +`region` / `location_count` / `region_id` on the web types, and i18n `Kawasan`/`Region`. Live region patches ride the existing `area:staffing-changed`→invalidate path; a direct `subscribe:region` is a 5.6b TODO. **Medium review caught three drill bugs** the subagent introduced (unit-green but wrong): `rayonAgg` was keyed by `view.id` at region/area scope (a region/area id, not a rayon id) → wrong/failing queries; `drillToArea` didn't carry `regionId`, so back-from-area skipped the kawasan tier; and the marker-drill passed `view.id` as `rayonId` (a region id at region scope). All fixed. ⚠️ **Browser-pending:** the interactive drill + pan/zoom were verified via tsc/unit/i18n only — actual in-browser drill behaviour and marker rendering still need a real browser pass (5.6b covers the Advanced-Markers perf rewrite).
- 2026-07-16 — **Phase 5.5c — scope enforcement on the region aggregate.** The new `getAggregate('region', rayonId)` path is now scope-enforced exactly like `rayon`: a district role (kepala_rayon / admin_rayon) is forced to its own rayon and can't view another rayon's kawasan rollup; a city role may target any rayon. Swagger `scope` enum + description updated to include `region`. **Deferred (documented, not a security hole):** replacing the hardcoded `enforceScopeRayon`/`enforceScopeArea` + `applyScopeFilters` role lists with a data-driven `role.monitoring_scope` lookup (ADR-044) is a behaviour-preserving refactor — the current hardcoded guards already enforce correctly, and there is no `region`-scope *role* yet, so the room-join auto-subscribe for one stays parked too. Controller spec covers region forced-to-own-rayon + city-can-target-any.
- 2026-07-16 — **Phase 5.5b — `monitoring:region:{id}` WebSocket room** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md) realtime). The map can now receive live patches while drilled into a kawasan: a `subscribe:region` / `unsubscribe:region` client handler (mirroring `subscribe:area`/`:rayon`), an optional `region_id` on the monitoring event payloads (populated in the status-calculator broadcast helpers from `context.area.region_id`; `resolveUserContext` + `emitStaffingChangedIfNeeded` selects updated), and a region broadcast added to the 8 relevant emit methods (location, clock-in/out, status-changed, left/entered-area, reassigned, staffing-changed) — guarded by `if (event.region_id)`, so unpopulated events are unaffected. `region_id` is **optional** on every payload, keeping it additive. Room-join selects `region_id` in prep; auto-joining a `monitoring:region` room for a REGION-scope role is deferred to 5.5c (no role uses that scope yet). The web `subscribe:region` call on kawasan drill lands with 5.6. Gateway spec covers subscribe + the region broadcast.
- 2026-07-16 — **Phase 5.5a — region (kawasan) aggregate tier** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md) drill Rayon→Region→Location). Additive `'region'` scope on `getAggregate`: `getAggregate('region', rayonId)` returns one node per active kawasan in the rayon (`type: 'region'`, with `location_count`), mirroring how city scope returns rayon nodes — grouped by `area.region_id`, staffing measured on satgas+linmas ∩ scheduled (ADR-050). Area nodes now also carry `region_id` so the web can group locations under their kawasan. The three group helpers (`requiredCountByGroup` / `scheduledUserSetsByGroup` / `countableOnlineByGroup` / `presenceByGroup`) gained a `region` branch; existing city/rayon/area behaviour is unchanged. `Region` repo injected into the monitoring module. **Medium review caught two bugs the mocked query-builder specs couldn't:** the refactor dropped `countableOnlineByGroup`'s early-return-on-empty guard (an empty id list would have scanned the whole DB unfiltered — masked today only because callers guard empty), and `presenceByGroup` used the invalid alias `uts.area.region_id` (would throw against real Postgres); both fixed. Still to come in 5.5: `monitoring:region:{id}` WS room + server-side `monitoring_scope` enforcement.
- 2026-07-16 — **Phase 5.4e — occurrence-driven geofence for mobile crews** ([ADR-046](../../architecture/decisions/ADR-046-monitoring-subject-model.md) static-vs-mobile; matrix Layer 4/geofence). A worker with **no lokasi assignment** whose occurrence is region-scoped now geofences against their **kawasan** polygon, not a lokasi — "outside area" for a mobile subject means outside its kawasan. `checkWithinAnyAssignedArea` keeps the lokasi-only rule for static workers (outside every assigned lokasi = outside) and only falls to the region when there is no lokasi context (`resolveMobileRegion` reads today's/yesterday's occurrence `region_id`, yesterday covering a Shift-3 crew past WIB midnight). The boundary cache/loader is generalised to any scope — `getBoundary('location'|'region'|'rayon', id)`, cached per `scope:id`, table chosen from a fixed allowlist (no SQL injection); `getAreaBoundary` stays as a back-compat alias. The tolerance math (`BoundaryCheckService.isPointInPolygonWithTolerance`) is reused unchanged. Fails open when a kawasan has no polygon, matching the lokasi rule. **Deferred (documented):** rayon-scoped and supervisor (kepala_rayon/admin_rayon → `users.rayon_id`) geofencing — the region/rayon plumbing is in place, so it's a small follow-up. Test: a mobile crew outside its clock-in lokasi but inside its kawasan stays within-area.
- 2026-07-16 — **Phase 5.4d — ad-hoc clock-ins excluded from staffing counts** ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md) counting / Q12; matrix Layer 3). `countableOnlineByGroup` now returns the **set of online satgas+linmas user ids** per group instead of a raw count, and the aggregate callers intersect it with the already-computed `scheduledUserSetsByGroup` — so `is_understaffed` weighs only workers who are **scheduled for this subject**. A satgas who clocks in ad-hoc (no occurrence here today) still renders on the map with its distinct marker but no longer fills the requirement, closing the gap where a passer-by masked a real shortfall. Keying is physical-online ∩ scheduled-here, so a worker scheduled elsewhere doesn't count here either. The city/rayon aggregate spec now demonstrates it directly (6 online, 3 rostered → 3 < 8 required → understaffed). **Deferred (documented):** the snapshot side-panel `buildAreaSummaries.active_count` still counts by role only, not roster — a separate follow-up, since excluding ad-hoc there needs its fixtures to populate the scheduled set; the ADR-046 bubble understaffing (this change) is the canonical signal.
- 2026-07-16 — **Phase 5.4c — lifecycle surfaced on the snapshot** ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md); matrix Layer 3). Live (bertugas) workers now carry `lifecycle_state` · `is_late` · `lifecycle_flags` on `SnapshotWorker` + `LiveUserDto` (+ optional on the web/mobile snapshot-worker types, ready for 5.6 rendering — no rendering yet). `getLiveUsers` resolves each worker's shift window on the clock-in's WIB service day (cross-midnight safe) and runs the 5.4b engine to derive `is_late` / `lupa_clock_out` / `lembur`; `ad_hoc` is appended by `computeSnapshot`, where the current-shift roster check lives. Grace flows through `StatusThresholds.late_grace_seconds` (global `monitoring.late_grace_sec`, default 900). Answers Q4 (is he late) and the Q12 ad-hoc marker. Defensive `?? []` / `?? 'bertugas'` guards keep partial rollout payloads safe. Tests: is_late wiring under frozen time, ad_hoc appended only when off-roster, `bertugas` always present.
- 2026-07-16 — **Phase 5.4b — presence lifecycle engine (pure)** ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md); test matrix Layers 1/G/X). New `monitoring/lib/presence-lifecycle.ts`: `derivePresenceState(facts, now)` returns one of the six lifecycle states + flags (`is_late`/`ad_hoc`/`lupa_clock_out`/`lembur`/`early`/`excused`) as a **pure** function of schedule + clock in/out + the resolved shift window — nothing stored, so it can't drift like the 5.3 enum. Works on **resolved instants** (via `resolveShiftWindow`, which applies cross-midnight for shift 3), so "late / past-end / before-start" are plain timestamp comparisons that are correct for night shifts without the old `HH:mm` noon heuristic. Grace is a parameter (per-shift override lands in 5.4c); a global default `monitoring.late_grace_sec` (900 s / 15 min) is added to the settings catalog. 26 unit cases incl. the full PM-L truth table, grace mechanism, cross-midnight PM-X01/X02, and a totality property test. No snapshot wiring yet — that is 5.4c.
- 2026-07-16 — **Phase 5.4a — offline threshold 5 → 10 min** ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md) policy 4). `monitoring.active_max_age_sec` default 300 → **600** across every surface (settings catalog, config-service loader, cache defaults, seed, DTO examples). Resolves both 5.3 follow-ups: the sweeper's 5-min interval now sits safely **below** the 10-min threshold (backstop lag gone), and the unreachable-worker alert fires after 10 min (not 5) — already de-duped to once per worker per Jakarta day, so no korlap spam. The legacy `monitoring-config` zod ceiling was widened 600 → 1800 for consistency (that path no longer writes this key — thresholds are `/settings`-owned, catalog max 86400, so headroom already existed). Gating the alert to *within the shift window* is a lifecycle-phase (5.4b) refinement — it needs the resolved shift end this path doesn't yet carry.
- 2026-07-16 — **Presence & attendance model standardised ([ADR-050](../../architecture/decisions/ADR-050-presence-attendance-model.md), feeds Phase 5.4).** Stress-testing the 5.3 three-value status against the twelve questions management actually asks (where is he · scheduled · clocked in · late · why-not / who · active-or-offline · inside-or-outside · clocked-out · pulang-vs-offline · where-now · where-last-active · unscheduled-but-online) showed one flat `status` was still answering three unrelated questions, so `absent` meant no-show + went-home + off-today at once — leaving four hard gaps (late, *pulang*, absence-reason, ad-hoc counting). Resolved by splitting into **three derived axes** — attendance lifecycle (`tidak_bertugas`/`belum_hadir`/`terlambat`/`bertugas`/`pulang`/`tidak_hadir`, one per worker per **service day**, only `bertugas` renders live), live presence (`aktif`/`offline` + inside/outside axis + one position field: current when aktif, last-known when offline), and counting (`bertugas` ∧ scheduled-for-subject ∧ satgas/linmas; ad-hoc + monitorable roles show with a distinct marker but never count). **Store facts, derive the state** — no persisted lifecycle enum, so the drift that caused the 5.3 `offline`↔`absent` inversion can't recur. Reuses what already exists (`is_late`, `clock_out_time`, the Phase-4 roster) — mostly surfacing, not new storage. **Four policies locked:** per-shift-definition grace (default late = 15 min after start, no-show = at shift end); forgotten clock-out **never auto-closed** (flagged `lupa_clock_out` until a supervisor corrects it — lembur is explicit-only, judged on the shift's *real* end so shift 3 past midnight stays `bertugas`); excused reasons cuti/sakit/izin/libur set by admin_rayon/korlap; **offline threshold 10 min** (`active_max_age_sec` 300→600, catalog max raised for headroom), korlap paged at threshold but only within the shift window and once per transition. Guardrails: everything scoped to the current service day (a dangling shift never contaminates today), a new clock-in blocked only by a *within-window* open shift, and a supervisor "close & stamp" affordance is a required companion since forgotten clock-outs never auto-close.
- 2026-07-16 — **Phase 5.3 — status model collapsed 5 → 3** (`active` · `offline` · `absent`), amending ADR-046's *"no new status code"*. **`offline` inverted meaning**: it meant *not clocked in*, it now means *clocked in but unreachable*; the old `offline` becomes **`absent`** (*tidak hadir* where a schedule exists). `inactive`/`idle` and `missing` both fold into `offline`, and **`outside_area` stops being a status** — it is now an axis (`is_within_area`) shown *alongside* active/offline, per the request that both states still report inside/outside. The inversion drove two decisions worth recording. **(1) The migration remaps in ONE `CASE` pass, never a sequence** — `missing→offline` followed by `offline→absent` would re-migrate the rows the first statement had just written, silently converting unreachable workers into absent ones; verified against the live DB with a planted mixed fixture (`missing=2, offline=41` → `offline=2, absent=41`, not `absent=43`), plus constraint rejection and a round-trip through `down()` (which is lossy by construction: the old vocabulary returns, the old distinctions cannot). **(2) `outside_area` now OVERLAPS the statuses instead of partitioning them**, so every site that summed it was audited — `is_fully_staffed` and `assembleNode` would have double-counted anyone outside their boundary and reported a short-staffed park as fully staffed. Two meanings of "online" were made explicit rather than left to collide: the **display** pair partitions clocked-in workers into reachable/unreachable, while **staffing counts whoever clocked in** (`active + offline`) — a park is no less staffed because a phone lost GPS; `countableOnlineByGroup` now just filters `shift_id IS NOT NULL`, since clocking in *is* the test. Retires `monitoring.inactive_threshold_sec` + `missing_threshold_sec` (catalog, envKeys, schema, cache, seed, and the stale `MISSING_THRESHOLD_SECONDS` in `.env.local`) — once idle and missing both fold into offline, nothing could reach them; **`active_max_age_sec` (300 s) is the single surviving boundary**. i18n `status:tracking.*` was **re-copied, not just re-keyed** on both platforms: "Offline"/"Offline" previously labelled *not clocked in* and now labels its opposite — `i18n:check` enforces key parity and cannot see that. **Two follow-ups flagged, not silently absorbed:** the unreachable-worker alert used to fire on `missing` (1 h of silence) and now rides `offline` (5 min), so it triggers **12× sooner** and wants a dwell timer or retirement; and the stale sweeper's 5-min interval now *equals* the threshold it enforces (was 12× shorter), leaving up to ~10 min of backstop lag.
- 2026-07-16 — **Phase 5.0 — staffing correctness (a live regression, backend only).** Two defects, both invisible to a green suite because no fixture ever crossed a supervisor with a requirement, or put a requirement anywhere but a lokasi. (1) **`requiredCountByGroup` dropped 76% of the city's target.** Requirements are polymorphic (Phase 4 / ADR-045) — exactly one of `location_id`/`region_id`/`rayon_id` is set per the rayon's `staffing_level` — but the rayon rollup did `innerJoin('req.area')` and grouped by `area.rayon_id`, so **every row with a NULL `location_id` vanished**. Once Phase 4 (#231) moved the workbook targets to the **kawasan** tier, monitoring summed **245 of 1033** required and reported `required: 0` for **8 of 9 rayons** — their bubbles could never read understaffed. Now LEFT JOINs all three tiers and groups on `COALESCE(loc.rayon_id, reg.rayon_id, req.rayon_id)`. **Live-verified: all 8 rayons now resolve** (Barat 1 = 95, Pusat = 190, Taman Aktif = 245 …) where only Taman Aktif did before. (2) **Supervisors counted as staff.** `is_understaffed` compared *every* monitorable role's online count against a satgas+linmas requirement, so a korlap standing in a park masked a real shortfall. New **`STAFFING_COUNTED_ROLES`** (satgas+linmas) beside the existing `CLOCKABLE_ROLES` — which is already ADR-046's *monitorable* set under an older name — mirroring the web day board's `COUNTABLE_ROLES` so the board and the map report **one number for one place**. `counts_by_status` deliberately stays all-roles (it is what the bubble *displays*); only the comparison narrows, via a new `countable_online` figure. **`area`-tier grouping stays location-keyed on purpose**: a lokasi under a kawasan-scoped rayon genuinely has no target of its own — the kawasan owns it, exactly as the day board renders it. The kawasan's own bubble is the region tier (5.5). Every new test was confirmed to **fail against the old code** before being kept. Verified: be 148 suites/**2406** (+3), tsc/eslint clean.

- 2026-07-12 — **Area→Location terminology sweep.** Renamed drill hierarchy, active-inside-location, LocationStaffRequirement; keep monitoring WS room prefixes (`area:{id}`).
- 2026-07-10 — Revamp target set: subject model, static/mobile, drop Surabaya bubble, team bubbles, region tier, search (ADR-046).
- 2026-07-10 — Spec created in product-docs restructure. History: [`../../history/CHANGELOG.md`](../../history/CHANGELOG.md).
