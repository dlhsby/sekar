# Hex color permanent allowlist for eslint-plugin-sekar-design/no-inline-hex-colors
#
# Files listed here contain hex literals that CANNOT be replaced with design tokens
# because the color has no token equivalent, or the file IS the token source itself.
#
# Format: <file path relative to repo root> | <reason>
#
# Review during each Phase 3 sub-phase sweep; remove entries as tokens are added.

# ── Token source files (hex IS the definition — never replace) ────────────────
fe/mobile/src/constants/nbTokens.ts        | Token re-export + backward-compat shims; hex values come from generated/tokens.ts
fe/mobile/src/constants/generated/tokens.ts | Generated by scripts/build-tokens.ts — do not edit; hex is the canonical source
fe/mobile/src/utils/nbShadow.ts             | Shadow helper reads shadow token hex directly

# ── Map & geospatial UI (no token equivalents for these specific colors) ──────
fe/mobile/src/utils/mapUtils.ts             | Status marker palette: #9333EA (outside_area/purple), #D97706 (inactive/amber-600), #DC2626 (missing/red-600), #6B7280 (offline/cool-gray) — all deliberately distinct on maps; purple + cool-gray have no NB token
fe/mobile/src/components/monitoring/LocationTrail.tsx | OUTSIDE_COLOR #9333EA (purple trail — no token), #4338CA (indigo-700 speed label — no token)
fe/mobile/src/components/monitoring/BoundaryOverlay.tsx | rayonCenterMarker backgroundColor #2563EB (blue-600 rayon dot — no NB blue token)

# ── Legacy Phase 2 common components (not yet migrated to NB primitives) ──────
fe/mobile/src/constants/theme.ts            | Phase 2 legacy theme still imported by components/common/{Button,Card,Input,EmptyState,SkeletonLoader,TextInput,SyncStatusIndicator,LoadingSpinner}; sweep those callers → remove this entry in 3-R5 continuation or Phase 4
fe/mobile/src/services/sync/INTEGRATION_EXAMPLE.tsx | Documentation/example file, not production code; migrate or delete in Phase 4 cleanup

# ── Web: Next.js metadata + server-side image rendering ───────────────────────
fe/web/src/app/layout.tsx                   | Next.js themeColor metadata must be a literal string (browser meta tag, not CSS)
fe/web/src/app/icon.tsx                     | ImageResponse server-side SVG generation — CSS vars are not resolved in this context
fe/web/src/app/apple-icon.tsx               | Same as icon.tsx

# ── Web: Monitoring status palette (monitoring v2 will address in 3-3/3-4) ────
fe/web/src/lib/constants/monitoring.ts      | STATUS_COLORS palette: #9333EA (outside_area/purple) + others have no NB token; this file feeds all monitoring components
fe/web/src/components/monitoring/StaffingSummaryCard.tsx | Uses STATUS_COLORS palette from monitoring.ts
fe/web/src/components/monitoring/StatusCard.tsx          | Status badge colors from monitoring palette
fe/web/src/components/monitoring/UserDetailPanel.tsx     | #374151 (Tailwind gray-700, cool-gray) not in warm-stone NB palette
fe/web/src/components/monitoring/LocationTimeline.tsx    | #14532D (green-900), #581C87 (purple-900) — no NB token equivalents

# ── Web: Mapbox GL layer paint specs (CSS vars unsupported in GL style expressions) ─
fe/web/src/components/maps/PolygonEditor.tsx             | Mapbox GL layer paint property; CSS vars are not supported in GL style spec
fe/web/src/components/monitoring/MonitoringMap.tsx       | Same
fe/web/src/components/monitoring/monitoringMapHelpers.ts | Same
fe/web/src/lib/maps/styles.ts                            | Same
fe/web/src/lib/utils/static-map.ts                      | Static map URL builder with hex encoded in URL params

# ── Web: Test files (hex in assertions, not component code) ──────────────────
fe/web/src/lib/api/__tests__/rayons.test.tsx | Hex in test assertions that validate rendered color values
fe/web/src/lib/maps/__tests__/styles.test.ts | Same
